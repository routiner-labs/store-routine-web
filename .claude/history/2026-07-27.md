# 2026-07-27

## 작업 규칙 및 개발 환경 준비

- 작업한 파일: `.claude/good-bad.md`, `.claude/history/2026-07-27.md`
- 변경 이유: `mockup_v2_codex` 브랜치에서 사용할 응답 언어, 작업 권한, 커밋 방식을 명시하기 위해 작업 규칙을 추가했다.
- 주요 결정 사항: AI 응답은 한글로 작성하고, 작업은 검증 가능한 소단위로 나누며, 각 단위가 완료될 때 `feat: {작업내역 한글}` 형식으로 커밋한다.
- 화면/기능: 제품 화면 변경 없음. WSL 개발 서버는 Windows의 3000번 포트와 충돌하지 않도록 3005번 포트로 실행한다.

## 사장 스케줄 관리 본문 폭 확장

- 작업한 파일: `src/app/owner/attendance/EmployeeScheduleManager.module.css`, `.claude/history/2026-07-27.md`
- 변경 이유: 데스크톱 스케줄 관리 화면의 본문이 860px로 제한되어 남는 웹 콘텐츠 폭을 활용하지 못하던 문제를 해소했다.
- 주요 결정 사항: 768px 이상에서만 `.body`의 `max-width`를 해제해 모바일 레이아웃은 유지하고, 기존 데스크톱 행 그리드 배치를 그대로 사용한다.
- 검증: 기본 스케줄과 일자별 조정 화면에서 본문 폭이 루트 폭과 일치함을 확인했고, 390px 모바일 화면에서 가로 스크롤이 없음을 확인했다. 라이트·다크 조건의 데스크톱/모바일 캡처로 본문 폭과 컨트롤 배치를 확인했다.

## 사장 스케줄 관리 반응형 편집표

- 작업한 파일: `EmployeeScheduleTable.tsx`, `EmployeeScheduleTable.module.css`, `EmployeeScheduleManager.tsx`, `EmployeeScheduleManager.module.css`, `.claude/history/2026-07-27.md`
- 변경 이유: 기본 스케줄과 일자별 조정의 직원별 설정을 열 헤더가 있는 편집표로 정렬하고, 태블릿과 모바일에서 문서 가로 넘침 없이 편집할 수 있도록 개선했다.
- 주요 결정 사항: 1024px 이상은 로컬 가로 스크롤 가능한 표면 카드와 모드별 열 너비를 사용하고, 768~1023px은 직원 정보와 설정·시간을 두 줄로 재배치한다. 기존 생성·삭제·요일·시간·근무/휴무·기본 복귀 이벤트는 유지했다.
- 검증: 임시 Playwright 명세에서 헤더·열 간격·1024/768/390px 가로 넘침 5건을 확인하고, 라이트·다크 및 주요 뷰포트에서 화면을 점검했다.

## 스케줄 편집표 최종 리뷰 품질 개선

- 작업한 파일: 스케줄 편집표·관리자 컴포넌트와 스타일, `tests/e2e/schedule-management.spec.js`, 작업 이력 및 결과 보고서.
- 변경 이유: 편집표의 표 의미와 직원별 접근 가능한 이름을 보완하고, 반응형 규칙의 책임과 CSS 가독성을 정리했다.
- 주요 결정 사항: 표·행·열·셀 역할과 동적 `aria-pressed`를 기존 상태에 연결했으며, 모바일 날짜 영역은 기존 평면 흐름을 유지하고 768px 이상에서만 카드 표면을 사용한다.
- 검증: 격리된 3015 dev 서버에서 영구 Playwright 11건 통과, 라이트·다크 16개 화면 확인, 빌드·diff 검사 통과 및 모든 대상 소스 300줄 이하를 확인했다.

## 스케줄 저장 영역과 프로필 영역 정렬

- 작업한 파일: `OwnerNav.tsx`, `EmployeeScheduleManager.tsx`, `EmployeeScheduleTable.module.css`, `tests/e2e/schedule-management.spec.js`, `.claude/history/2026-07-27.md`.
- 변경 이유: 스케줄 저장 영역의 상단 경계와 사장 사이드바 프로필 영역의 상단 경계를 반응형 구간별로 일치시켰다.
- 주요 결정 사항: 두 영역에 검증용 식별자를 추가하고, 저장 영역 높이를 768~1023px에서 101px, 1024px 이상에서 59px로 적용했다. 768px 미만 모바일 높이와 배치는 변경하지 않았다.
- 검증: 식별자 부재로 정렬 테스트 4건이 실패하는 RED를 확인한 뒤 전체 Playwright 15건이 통과했다. 1440·1024·768px에서 두 영역의 위치와 높이가 정확히 일치하고, 390px에서는 기존 63px 저장 영역이 유지됨을 캡처로 확인했다.
- 검증 강화: 각 웹 구간의 59px·101px 절대 높이와 390px의 기존 63px 높이·우측 하단 배치를 명시적으로 검증하며, 전체 Playwright 16건이 통과했다.

## 사장 스케줄 직원 이름 검색

- 작업한 파일: `EmployeeScheduleManager.tsx`, `EmployeeScheduleTable.tsx`, `EmployeeScheduleTable.module.css`, `tests/e2e/schedule-management.spec.js`, `.claude/history/2026-07-27.md`.
- 변경 이유: 기본 스케줄과 일자별 조정에서 직원 이름 일부를 입력해 즉시 필요한 직원만 볼 수 있도록 했다.
- 주요 결정 사항: 검색어는 공백을 제거하고 한국어 로캘 소문자로 비교하며, 표시 목록에만 적용한다. 검색창은 공통 편집표 밖의 도구 영역에 두고, 빈 결과도 표 행·셀 의미를 유지한다. 숨긴 직원의 스케줄 상태와 저장 동작은 유지한다.
- 검증: 검색 UI 부재로 검색 E2E 3건이 RED 실패한 뒤 GREEN 3건 및 전체 Playwright 19건을 통과했다. 1440·768·390px, 라이트·다크에서 검색·빈 결과·초기화와 가로 넘침 없음을 확인했으며, scoped lint·diff 검사·격리 worktree 프로덕션 빌드가 통과했다.
