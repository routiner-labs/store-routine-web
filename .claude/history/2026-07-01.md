# 2026-07-01 작업 기록

## 83. 업무리스트 요약 바 클릭 필터

사용자 요청: 업무리스트 요약(전체/할당/미할당/완료/미완료) 클릭 시 해당 내역만 보이도록.

수정된 파일:
- `src/app/owner/checklists/page.tsx`
  - `SummaryFilter` 타입(`ALL|ASSIGNED|UNASSIGNED|DONE|PENDING`) + `summaryFilter` 상태
  - `matchesSummary(t)` — 필터에 맞는 업무만 통과
  - 요약 항목을 `<div>` → `<button>`, 활성 항목 강조, 클릭 시 필터 설정(활성 항목 재클릭 시 전체로 토글)
  - `renderSection` 필터에 `matchesSummary` 결합(검색과 AND), 빈 상태 문구에 "해당하는 업무가 없습니다"(필터 활성 시) 추가
- `src/app/owner/checklists/page.module.css` — `.summaryItem` 버튼화(리셋·hover), `.summaryItemActive`(primary 강조)

주요 결정:
- 요약 숫자는 항상 전체 집계 유지(필터는 목록 표시에만 적용) → 분포를 보면서 필터링
- 활성 필터 재클릭 = 전체로 해제(토글)
- 검색어(섹션별)와 요약 필터는 AND로 동작
- 빌드(`next build`) 성공 확인

## 84. 알림 패널 — 최상위 + 벨에 부착

사용자 요청: 알림 창은 항상 모든 창보다 최상위. 지금 알림창이 버튼과 조금 동떨어져 있음.

원인:
- `.nav`가 `position: sticky` → 독립 stacking context 생성. 패널이 nav 내부에 있어 z-index를 올려도 nav 밖 모달(300)/컨펌(500) 위로 못 올라감
- 패널이 `position: fixed`로 사이드바 가장자리(`bottom:72px; left:sidebar+8`)에 고정 → 벨과 동떨어짐

수정된 파일:
- `src/components/NotificationBell.tsx` — 패널/오버레이를 `createPortal(..., document.body)`로 body에 렌더(스택 컨텍스트 탈출). 벨 `ref`의 `getBoundingClientRect`로 위치 계산(`left: 벨 right+8`, `bottom: 벨 하단 정렬`)해 인라인 style로 지정. 열 때 위치 계산(`toggle`)
- `src/components/NotificationBell.module.css` — `.panel` z-index 200→1200(+포탈이라 실제 최상위), 뷰포트 고정/미디어쿼리 위치 계산 제거(인라인 위치로 대체), `.overlay` z-index 99→1199

주요 결정:
- 포탈 + z-index 1200 → 모달/컨펌 위 최상위(토스트 9999는 상단 유지 — 토스트는 '창'이 아님)
- 벨 rect 기준으로 붙여 "동떨어짐" 해소(벨 오른쪽 8px, 하단 정렬)
- 모바일은 기존 전체화면 알림(`notifScreen`, OwnerNav)이라 영향 없음
- 헤드리스 검증: 패널 body 직속·z-index 1200·position fixed·left=벨right+8·bottom=벨하단, 오류 0 / 빌드 성공
- 후속: 패널을 살짝 위로(벨 하단 정렬 + 14px) — `bottom: innerHeight - r.bottom + 14`

## 85. 업무리스트 날짜 네비 — 현황과 통일(헤더 밑·중앙)

사용자 요청: 업무리스트 날짜를 헤더에서 밑(요약 영역)으로 내리고 중앙 정렬. 현황 페이지 날짜가 중앙이라 디자인 통일.

수정된 파일:
- `src/app/owner/checklists/page.tsx`
  - 헤더는 제목("업무리스트")만 남기고 날짜 제거
  - 헤더와 요약 사이에 전체폭 날짜 바 추가(현황과 동일: `‹ 레이블 ›`, `justify-content: space-between`)
  - 미사용 `LiaAngleLeftSolid`/`LiaAngleRightSolid` import 제거(‹ › 문자로 대체 — 현황과 동일)
- `src/app/owner/checklists/page.module.css`
  - `.dateNav`/`.dateBtn`/`.dateLabel` → `.periodNav`/`.navBtn`/`.periodLabel` (현황 `page.module.css` 스타일과 동일하게 복제)

주요 결정:
- 현황(attendance)의 `.periodNav`(헤더 아래 전체폭·중앙 레이블) 디자인을 그대로 맞춰 두 페이지 통일
- 업무리스트는 픽커 없이 일 단위 이동(goToDate)만 유지 — 시각적 통일이 목적
- 빌드(`next build`) 성공 확인
- (85는 다음 항목 86에서 방향 변경으로 대체됨)

## 86. 날짜를 헤더 안 중앙으로(현황·업무리스트 통일) — 방향 재변경

사용자 피드백: "저거는 아니다. 현황·업무리스트 날짜를 헤더에 넣자." (85의 헤더-아래 방식 취소)
AskUserQuestion 결과: 현황 = 제목(좌)·날짜(중앙)·뷰탭(우), 업무리스트 = 제목(좌)·날짜(중앙).

수정된 파일:
- `src/app/owner/attendance/page.tsx` — `periodNavWrap`(날짜 네비 + 픽커) 블록을 헤더 아래에서 헤더 안(h1 뒤, viewTabs 앞)으로 이동(awk 재배치)
- `src/app/owner/attendance/page.module.css`
  - `.periodNavWrap` → 헤더 안 절대 중앙(`position:absolute; top/left:50%; translate(-50%,-50%)`) — 헤더가 `position:sticky`라 containing block
  - `.periodNav` → 컴팩트(전체폭 바 스타일 제거: border-bottom/padding/space-between → `gap:4px`)
  - `.picker`는 기존 `top:100%` 유지 → 헤더 아래로 드롭
- `src/app/owner/checklists/page.tsx` — 날짜 네비를 헤더 아래에서 헤더 안(h1 뒤)으로 이동
- `src/app/owner/checklists/page.module.css` — `.periodNav`를 헤더 안 절대 중앙 + 컴팩트로 재스타일

주요 결정:
- 날짜는 sticky 헤더 기준 절대중앙 배치 → 좌우 요소(제목/뷰탭) 폭과 무관하게 정확히 중앙
- 헤드리스 검증: 두 페이지 모두 헤더 내부·중앙 offset 0, 현황 픽커 정상 오픈(top 55), 오류 0 / 빌드 성공
- 주의(미해결): 좁은 모바일 폭에서 현황 헤더(제목+중앙날짜+뷰탭)는 겹칠 여지 있음 — 필요 시 후속 조정

## 87. 날짜 좌우 버튼 제거 + 업무리스트도 달력 픽커

사용자 요청: 날짜의 좌우(‹ ›) 버튼 제거. 업무리스트도 날짜 누르면 현황처럼 달력 펼쳐지게.

수정된 파일:
- `src/app/owner/attendance/page.tsx` — periodNav의 ‹ › `navBtn` 2개 제거(레이블 클릭=픽커 유지). 고아가 된 `navigate`·`shiftDate`(연/월/일) 함수 제거
- `src/app/owner/attendance/page.module.css` — 미사용 `.navBtn`/`.navBtn:hover` 제거
- `src/app/owner/checklists/page.tsx`
  - 헤더 날짜: ‹ 레이블 › → 클릭 가능한 `periodLabelBtn`(▼) + 달력 픽커(월 이동 + 일 선택)
  - 헬퍼 교체: `shiftDate`(일 이동) 제거 → `formatDate`/`shiftMonth`/`getMonthGrid` 추가(현황과 동일 로직)
  - 함수 교체: `goToDate` → `openDatePicker`(선택일 기준 월 세팅) + `selectDate`(해당일 데이터 보장 + 선택 + 닫기)
  - 상태 추가: `datePickerOpen`/`pickerYear`/`pickerMonth`
- `src/app/owner/checklists/page.module.css` — 미사용 `.navBtn`/`.periodLabel` 제거, `.periodLabelBtn`·picker 계열(현황 스타일 복제) 추가

주요 결정:
- 좌우 일 이동 대신 달력에서 직접 날짜 선택(양 페이지 통일). 월 이동은 픽커 헤더의 ‹ ›로
- 현황의 picker 헬퍼는 페이지 로컬이라 업무리스트에 동일 로직 로컬 복제(소규모 순수 함수)
- 헤드리스 검증: 업무리스트 좌우버튼 0·달력 오픈(30일)·15일 선택 반영·닫힘, 현황 좌우버튼 0·달력 오픈, 오류 0 / 빌드 성공

## 88. 현황 뷰 선택 — 모바일 드롭다운

사용자 요청: 모바일 화면에서 출근 현황의 일간/주간/월간 선택을 드롭다운 메뉴로.

수정된 파일:
- `src/app/owner/attendance/page.tsx`
  - `VIEW_LABEL` 상수(day/week/month→일간/주간/월간), `viewMenuOpen` 상태 추가
  - 헤더에 모바일용 `viewDropdownWrap`(버튼 + 오버레이 + 메뉴 3항목) 추가, 기존 `viewTabs`는 데스크톱용으로 유지
- `src/app/owner/attendance/page.module.css`
  - `.viewTabs` 기본 `display:none`(모바일 숨김), `@media(min-width:768px)`에서 `flex`
  - `.viewDropdownWrap`은 모바일 표시·768px+ 숨김. `.viewDropdownBtn`/`.viewDropdownIcon`/`.viewMenu(Overlay/Item/ItemActive)` + `viewMenuIn` 애니메이션

주요 결정:
- 모바일=드롭다운(헤더 혼잡 완화, 86의 모바일 겹침 우려도 일부 해소), 데스크톱=기존 세그먼트 탭 유지 — CSS 미디어쿼리로 전환
- 헤드리스 검증: 모바일 탭 none/드롭다운 block·메뉴 3항목·선택 반영·닫힘, 데스크톱 탭 flex/드롭다운 none, 오류 0 / 빌드 성공
