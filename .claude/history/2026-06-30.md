# 작업 히스토리 - 2026-06-30

## 1. 프로젝트 초기 세팅

- Next.js 16.2.9 + React 19 + TypeScript 프로젝트 생성 (npm, App Router, CSS Modules)
- `.gitignore` 설정 — Next.js, TypeScript, IntelliJ/WebStorm, macOS, Windows 대응
- `.idea` 폴더 git 추적 제거 (`git rm -r --cached .idea`)

## 2. 에이전트 시스템 구축

`.claude/agents/` 폴더에 11개 에이전트 파일 생성:

| 파일 | 역할 |
|---|---|
| `planning-agent.md` | 기능 범위, 우선순위, 구현 순서 |
| `design-agent.md` | UI/UX 설계, 화면 구조 |
| `coding-agent.md` | 코드 구현 |
| `api-agent.md` | 백엔드 API 계약 정의 |
| `security-agent.md` | 인증·권한 분리·보안 검토 |
| `test-agent.md` | 테스트 케이스 정의 |
| `validation-agent.md` | 구현 결과 검증 |
| `refactoring-agent.md` | 코드 품질 개선 |
| `performance-agent.md` | PWA 성능 최적화 |
| `seo-agent.md` | SEO·메타데이터 |
| `documentation-agent.md` | 문서화 |

`.claude/CLAUDE.md`에 에이전트 참조 표와 작업 흐름 추가.

## 3. 프로토타입 제작 (API 없이 Mock 데이터 기반)

### 생성된 파일 구조
```
src/
├── types/index.ts          — 공통 TypeScript 타입 정의
├── mock/data.ts            — Mock 데이터 (직원, 체크리스트, 요청 등)
└── app/
    ├── page.tsx            — 역할 선택 화면 (사장 / 알바생)
    ├── owner/
    │   ├── layout.tsx      — 사장 레이아웃 + 사이드바 네비게이션
    │   ├── OwnerNav.tsx    — 네비게이션 컴포넌트 (client)
    │   ├── page.tsx        — 사장 홈 대시보드
    │   ├── checklists/     — 체크리스트 관리
    │   └── requests/       — 요청함 관리
    └── employee/
        ├── layout.tsx      — 알바생 레이아웃 + 네비게이션
        ├── EmployeeNav.tsx — 네비게이션 컴포넌트 (client)
        ├── page.tsx        — 알바생 홈
        ├── checklist/[id]/ — 체크리스트 수행 (상호작용)
        └── requests/new/   — 요청 등록 폼
```

### 화면 목록
| URL | 화면 |
|---|---|
| `/` | 역할 선택 |
| `/owner` | 사장 대시보드 — 출근 현황, 업무 진행률, 확인 필요 항목 |
| `/owner/checklists` | 체크리스트 전체 조회 |
| `/owner/requests` | 요청함 관리 |
| `/employee` | 알바생 홈 — 근무 시간, 오늘 할 일 |
| `/employee/checklist/[id]` | 체크리스트 수행 (체크·숫자·메모·사진 타입) |
| `/employee/requests/new` | 요청 등록 |

## 4. 반응형 대응

| 화면 크기 | 네비게이션 | 콘텐츠 |
|---|---|---|
| 모바일 (< 768px) | 하단 탭바 | 단일 컬럼 |
| 태블릿 (768px~) | 좌측 아이콘 사이드바 (72px) | 일부 2컬럼 그리드 |
| 데스크톱 (1024px~) | 좌측 라벨 사이드바 (220px) | 2컬럼 그리드, max-width 960px |

### 반응형 CSS 변수 추가 (globals.css)
- `--sidebar-sm: 72px`
- `--sidebar-lg: 220px`
- `--content-max: 960px`

## 5. 이모지 전면 제거

사용자 요청: 파일 작성 시 이모지 사용 금지 (AI스러워 보임) + 기존 파일 전체 처리.

변경된 파일:
- `src/app/page.tsx` — roleIcon 이모지(가게, 손) -> 텍스트(사장, 알바)
- `src/app/owner/OwnerNav.tsx` — icon 프로퍼티 제거, shortLabel/fullLabel 구조로 변경
- `src/app/employee/EmployeeNav.tsx` — 동일
- `src/app/owner/OwnerNav.module.css` — .icon 스타일 제거, shortLabel/fullLabel 추가
- `src/app/employee/EmployeeNav.module.css` — 동일
- `src/app/owner/page.tsx` — alertTitle 이모지 제거
- `src/app/owner/checklists/page.tsx` — completionIcon 함수를 completionTypeText 텍스트 함수로 교체
- `src/app/owner/requests/page.tsx` — requestTypeIcon 맵 제거, photoBadge 이모지 제거
- `src/app/employee/page.tsx` — checklistTypeIcon 맵 제거, 인사말 이모지 제거, 특별지시 아이콘 제거, 요청버튼 아이콘 제거
- `src/app/employee/checklist/[id]/ChecklistView.tsx` — 사진 버튼 이모지 제거, 완료 메시지 이모지 제거
- `src/app/employee/requests/new/page.tsx` — requestTypes icon 프로퍼티 제거, 성공 아이콘 제거, 사진 버튼 이모지 제거
- `.claude/CLAUDE.md` — 예시 코드 ❌/✅ 기호 -> [Bad]/[Good] 텍스트로 교체

주요 결정:
- 네비게이션 아이콘 없이 텍스트만 사용. 태블릿 사이드바(72px)에서는 단축 라벨(2자), 데스크톱/모바일에서는 전체 라벨 표시.
- 체크리스트 완료 타입은 이모지 대신 한국어 단어(확인, 사진, 숫자, 메모, 선택, 사장확인)로 표시.
- ✓ 체크마크(U+2713)는 이모지가 아닌 유니코드 기호이므로 체크버튼에서 유지.

## 6. 복수 매장 지원 + 출근 캘린더

### 복수 매장 지원

사장은 N개 매장을 운영 가능 → 현재 매장 선택/전환 기능 추가.

추가된 파일:
- `src/types/index.ts` — Store, CalendarRecord 타입 추가
- `src/context/StoreContext.tsx` — 현재 매장 상태 관리 (3개 모의 매장)
- `src/app/owner/StoreSwitcher.tsx` — 매장 전환 모달 (바텀시트/센터 모달)
- `src/app/owner/StoreSwitcher.module.css`
- `src/app/owner/StoreBar.tsx` — 모바일 상단 매장 표시 바
- `src/app/owner/StoreBar.module.css`

수정된 파일:
- `src/app/owner/layout.tsx` — StoreProvider 주입, StoreBar + content 래퍼 추가
- `src/app/owner/layout.module.css` — .content 래퍼 추가
- `src/app/owner/OwnerNav.tsx` — useStore, StoreSwitcher 연동 / 출근현황 nav 추가
- `src/app/owner/OwnerNav.module.css` — storeTrigger 스타일 추가
- `src/app/owner/page.tsx` — 'use client' 전환, useStore로 매장명 동적 표시

### 출근 캘린더

추가된 파일:
- `src/mock/calendar.ts` — 직원별 근무 스케줄 + 이상 케이스 오버라이드로 6월 모의 데이터 생성
- `src/app/owner/attendance/page.tsx` — 월별 달력 + 날짜 클릭 시 당일 출근 상세
- `src/app/owner/attendance/page.module.css`

### 주요 결정
- 모바일: 하단 탭 + 상단 StoreBar(매장명 표시) 분리. 태블릿 사이드바(72px): "▾" 아이콘만 표시. 데스크톱: 전체 매장명 + 화살표.
- 달력 모의 데이터: 4명 직원 × 요일별 스케줄 + 지각/결근 예외 케이스 하드코딩.
- 달력 셀: 당일 출근 인원수 표시, 지각/결근 있으면 빨간색. 셀 클릭 시 상세 패널.
- 매장 전환 시 같은 모의 데이터를 공유(프로토타입 범위).

## 7. 출근현황 뷰 개선

사용자 요청: 일간/주간/월간 탭 + 달력 전체 화면.

수정된 파일:
- `src/app/owner/attendance/page.tsx` — 전면 재작성
- `src/app/owner/attendance/page.module.css` — 전면 재작성

주요 변경:
- 헤더에 일간/주간/월간 세그먼트 탭 추가
- 월간: 달력 그리드 max-width 제거, 전체 너비 사용. 셀 min-height 68px(모바일) ~ 110px(데스크톱). 오늘 날짜 파란 원 표시.
- 주간: 7개 행 리스트. 요일·날짜 + 근무자 이름 태그(지각 주황, 결근 빨강 취소선) + 인원 수.
- 일간: 직원별 카드. 예정/출근/퇴근 시각, 상태 뱃지, 지각·결근 강조 표시.
- 월간/주간 공통: 날짜 클릭 시 하단 상세 패널 표시. 다시 클릭하면 닫힘.
- 뷰 전환 시 선택된 날짜 초기화(일간 전환 시에는 현재 날짜로 자동 설정).

## 8. 출근현황 브랜드 컬러 적용

디자인 에이전트에 정의된 루틴 블루 시스템을 출근현황 페이지 CSS에 반영.

수정된 파일:
- `src/app/owner/attendance/page.module.css`

변경 내용:
- `.tabActive`: 흰 배경 + 파란 텍스트 → 파란 배경(`--color-primary`) + 흰 텍스트. 활성 탭이 CTA처럼 명확하게 구별됨.
- `.tab:hover`: 회색 → `--color-primary-light` 배경 + `--color-primary` 텍스트.
- `.navBtn:hover`: 회색 → `--color-primary-light` 배경 + `--color-primary` 텍스트.
- `.calCellSelected:not(.calCellToday) .calNum`: 텍스트 색상만 → `--color-primary-light` 원형 배경 추가. 오늘(파란 원) > 선택(연파란 원) > 기본 계층 완성.
- `.weekRow:hover`: 회색 배경 → `--color-primary-light`.
- `.badge_CLOCKED_IN`: 하드코딩 `#EFF6FF` / `#1D4ED8` → `--color-primary-light` / `--color-primary` CSS 변수로 교체.
- `.sheetHandle`: 회색 → `--color-primary-light`. 바텀시트에 브랜드 터치 추가.

주요 결정:
- 하드코딩 블루값 제거 — 디자인 에이전트 문서의 시맨틱 토큰 우선.
- 달력 날짜 원형 계층: 오늘=채운 파란 원(blue-600+white), 선택=연파란 원(blue-50+blue-600), 일반=텍스트만.

## 9. 출근현황 달력 디자인 소프트닝

수정된 파일:
- `src/app/owner/attendance/page.module.css`

변경 내용:
- `.calGrid`: `border-radius: 12px; overflow: hidden; margin: 16px; box-shadow: 0 2px 16px rgba(61,99,221,0.08)` — 달력 전체를 블루 tinted 카드로 감쌈.
- 셀 테두리: `--color-border` 회색 → `--color-primary-light` 연파란 (calHead, calEmpty, calCell 전부).
- `.calHead`: 배경 `--color-primary-light`, 텍스트 `--color-primary` — 요일 헤더에 브랜드 컬러 적용.
- `.calHead` border-bottom: `rgba(61,99,221,0.2)` — 헤더와 셀 경계선 약간 더 진하게.
- `.calEmpty`: `opacity: 0.5` 제거 — 날짜 없는 빈 셀이 반투명하게 겹쳐 보이는 문제 해결.
- `.calCellToday`: 셀 전체 `rgba(61,99,221,0.04)` 아주 연한 블루 배경 추가.
- `.periodLabel`: `color: var(--color-primary)` — 기간 표시 텍스트에 브랜드 컬러.
- `.weekDayToday`: `background: --color-primary-light; border-radius: 8px` — 주간 뷰 오늘 날짜 pill 표시.

주요 결정:
- 날카로운 회색 그리드 대신 연파란 테두리 + 카드 래퍼로 "부드러운 블루 달력" 느낌.
- `calEmpty`의 `opacity: 0.5` 제거 — 반투명이 오히려 어색하게 보였음. 단순히 배경색 차이로 빈 셀 구분.

## 10. 달력 gap 기반 그리드로 전환

사용자 피드백: "별로다, 더 이쁘게 만들어봐" — 테두리 기반 그리드가 근본적으로 딱딱해 보임.

수정된 파일:
- `src/app/owner/attendance/page.module.css`

핵심 변경:
- `.calGrid`: `border-top/border-left` 제거 → `gap: 1px; background: rgba(61,99,221,0.15)`. 셀 테두리 없이 그리드 배경색이 1px 간격으로 노출되어 구분선 역할. `box-shadow` 강화(20px, 10% opacity).
- `.calHead`: 테두리 전부 제거. 배경색 + 글자색만으로 헤더 구분.
- `.calEmpty`, `.calCell`: 테두리 전부 제거. 배경색 차이(gray vs white)로 셀 구분.
- `.calCell`: padding 미세 조정 (7px → 8px).
- `.calCellSelected:not(.calCellToday) .calNum`: `background: #fff; box-shadow: 0 1px 4px rgba(61,99,221,0.2)` — primary-light 배경 위에 흰 원이 떠 있는 효과. 선택 상태가 명확하게 구별됨.

주요 결정:
- 테두리 기반 → gap 기반으로 완전 전환. 이 방식은 셀 간 구분을 그리드 컨테이너 배경색으로 처리해 "딱딱함" 없이 구조감을 준다.
- `overflow: hidden + border-radius: 12px` 조합으로 모서리 클리핑 처리 — 개별 셀에 radius 없이도 카드형 달력 완성.
- 선택 날짜의 흰 원: primary-light 배경 위에서 대비를 만드는 가장 간결한 방법.

## 11. 직원 관리 기능

사용자 요청: 직원 초대, 직원 관리(재직/퇴직), 스케쥴 관리.

### 추가된 파일

| 파일 | 설명 |
|---|---|
| `src/types/index.ts` | `EmploymentStatus`, `WeeklySchedule`, `Employee`, `JoinRequest` 타입 추가 |
| `src/mock/employees.ts` | 직원 4명(재직 3, 퇴직 1) + 가입신청 2건 목 데이터 |
| `src/app/owner/employees/page.tsx` | 직원관리 페이지 |
| `src/app/owner/employees/page.module.css` | 직원관리 스타일 |

### 수정된 파일

| 파일 | 변경 내용 |
|---|---|
| `src/app/owner/OwnerNav.tsx` | 직원관리 nav 항목 추가 |
| `src/app/owner/OwnerNav.module.css` | 모바일 기본 표시를 fullLabel → shortLabel로 변경 (5개 항목에서 긴 라벨 넘침 방지) |

### 구현 내용

**직원 탭**
- 재직중/퇴직 필터 chip
- 직원 카드: 아바타(이름 첫 글자) + 이름/전화번호 + 상태 뱃지 + 요일 dot + 시간 범위
- FAB: "직원 초대" 버튼

**가입신청 탭**
- 신청 카드: 이름 + 전화번호 + 날짜 + 메시지
- 수락/거절 버튼 (클릭 시 목록에서 제거)
- 탭 뱃지: 신청 건수 표시

**직원 상세 시트 (바텀시트)**
- 상태, 입사일, (퇴직일), 근무 스케쥴 요일 dot + 시간 범위
- "스케쥴 수정" → 시트 내 스케쥴 수정 뷰로 전환 (요일 토글 + 시간 입력)
- "퇴직 처리" → 상태 INACTIVE로 변경, 퇴직일 설정

**초대 시트**
- 전화번호 입력 → "초대장 보내기" (10자리 미만 비활성)
- 전송 후 1.8초간 성공 메시지 표시 후 자동 닫힘

### 주요 결정
- 별도 페이지(/employees/[id]) 없이 하나의 페이지에서 바텀시트 패턴으로 모든 상세 처리 — 출근현황과 동일한 UX 패턴.
- 스케쥴 수정: 새 시트 열지 않고 기존 시트 내 뷰 전환. 상태 관리 복잡도 낮춤.
- `WeeklySchedule.days`는 0=월~6=일 인덱스로 기존 calendar.ts의 EMPLOYEES 정의와 동일.
- 모바일 nav shortLabel 전환: 5개 항목에서 "체크리스트" 등 긴 라벨이 75px 공간에 넘칠 수 있음. shortLabel(2자)로 기본 변경.

## 12. 직원 상세 페이지 + 카드 목록 재설계

사용자 요청: 상세화면 전체 크기로, 정보 입력 가능(이름/연락처/생년월일), 목록은 카드 형태.

추가된 파일:
- `src/app/owner/employees/[id]/page.tsx` — 직원 상세/수정 페이지
- `src/app/owner/employees/[id]/page.module.css`

수정된 파일:
- `src/types/index.ts` — Employee에 `birthDate?: string` 추가
- `src/mock/employees.ts` — 직원별 생년월일 추가
- `src/app/owner/employees/page.tsx` — 시트 제거, router.push로 상세 이동, 카드 그리드 전환
- `src/app/owner/employees/page.module.css` — 카드 그리드 레이아웃(모바일 1열, 640px+ 2열, 1024px+ 3열)

주요 결정:
- 목록의 직원 카드: 아바타(52px) + 이름/연락처 + 상태 뱃지 + 스케쥴 푸터. hover 시 2px lift + shadow 강화.
- 상세 페이지 구조: iOS 설정 앱 스타일. 섹션 구분(기본정보/근무정보/스케쥴). 필드마다 하단 border로 구분.
- 저장 버튼: 헤더 우측 + 하단 모두 배치. 클릭 시 "저장됨" 2초 피드백.
- 퇴직 처리: 상세 페이지 내 status 상태를 INACTIVE로 변경(프로토타입 내 상태만 변경, 목록에 미반영).

## 13. 직원 상세 페이지 데스크탑 2-컬럼 레이아웃

사용자 요청: 상세화면이 너무 모바일 기준이다. 웹은 전체 화면 디자인으로.

수정된 파일:
- `src/app/owner/employees/[id]/page.tsx` — layout/leftPanel/rightPanel 구조로 재편
- `src/app/owner/employees/[id]/page.module.css` — 반응형 2-컬럼 레이아웃 추가

주요 결정:
- 모바일: 기존 세로 스택 유지 (iOS 설정 스타일)
- 태블릿(768~): max-width 640px 중앙 정렬 + 섹션 카드화
- 데스크탑(1024+): 2-컬럼 — 좌측 280px sticky 프로필 카드 + 우측 폼 섹션
- 좌측 패널: 아바타(80px 중앙), 이름, 상태 뱃지, 연락처/입사일/근무시간 메타 표, 저장/퇴직처리 버튼
- 우측 패널: 필드 수평 배치(라벨 72px 고정 + input flex), 섹션 border-radius 카드화
- max-width: 1280px 레이아웃 컨테이너로 전체 화면 활용

## 14. react-icons(lia) 아이콘 적용

사용자 요청: 이모지 대신 react-icons/lia 아이콘 활용.

패키지 추가: `react-icons`

수정된 파일:
- `src/app/owner/OwnerNav.tsx` — 각 nav 항목에 LIA 아이콘 추가
- `src/app/owner/OwnerNav.module.css` — 모바일: 아이콘+텍스트(column), 태블릿: 아이콘만, 데스크탑: 아이콘+라벨(row)
- `src/app/owner/employees/[id]/page.tsx` — 뒤로가기/저장/필드라벨/퇴직버튼 아이콘 추가
- `src/app/owner/employees/page.tsx` — FAB 아이콘(LiaUserPlusSolid), 수락/거절 아이콘

사용 아이콘:
- LiaHomeSolid, LiaClipboardListSolid, LiaInboxSolid, LiaCalendarSolid, LiaUsersSolid (nav)
- LiaAngleLeftSolid (뒤로가기), LiaSaveSolid (저장), LiaUserMinusSolid (퇴직처리)
- LiaPhoneSolid, LiaBirthdayCakeSolid, LiaCalendarSolid, LiaClockSolid (필드 라벨)
- LiaUserPlusSolid (FAB), LiaCheckSolid, LiaTimesSolid (수락/거절)
- LiaAngleDownSolid (스토어 스위처 chevron)

## 15. 요청함 카드/리스트 뷰 토글

사용자 요청: 요청함에 카드 형태 + 리스트 형태 두 가지 뷰 제공.

수정된 파일:
- `src/app/owner/requests/page.tsx` — 'use client' 전환, 뷰 토글 상태 추가
- `src/app/owner/requests/page.module.css` — 카드 그리드 + 리스트 행 레이아웃

주요 결정:
- 헤더 우측에 토글 버튼 2개 (LiaThLargeSolid / LiaListSolid 아이콘)
- 카드 뷰: 기존 카드 스타일 유지. 모바일 1열, 768px 2열, 1024px 3열 그리드.
- 리스트 뷰: 유형(76px) | 내용(flex, 말줄임) | 요청자(72px) | 시간(52px) | 상태(72px) 5-컬럼 행. 상단 컬럼 헤더 포함.
- 섹션(미확인/처리중완료) 구분은 두 뷰 모두 유지.

## 16. 요청함 헤더 통일 + 반응형 풀스크린 레이아웃

사용자 요청: 출근현황/직원관리와 동일한 헤더 디자인. 웹/모바일 반응형 레이아웃.

수정된 파일:
- `src/app/owner/requests/page.tsx` — 컴포넌트 구조 정리
- `src/app/owner/requests/page.module.css` — 헤더 패턴 통일, 반응형 레이아웃

헤더 패턴 통일:
- sticky top:0, padding: 16px, background: surface, border-bottom
- heading (좌) + viewToggle (우) — 출근현황 탭 위치와 동일

반응형:
- 모바일: 카드 1열 / 리스트 컴팩트 행
- 태블릿(768px+): body max-width 1280px 중앙 정렬, 카드 2열
- 데스크탑(1024px+): 카드 3열, 리스트 컬럼 더 넓게
- 태블릿에서 header에 border-radius 카드화 제거 (다른 페이지와 불일치했던 부분 수정)

## 17. 요청함 상세페이지 + 답변 기능 + visibility

사용자 요청: 기본 리스트 뷰, 공개범위 설정, 상세 페이지(게시글), 답변 기능.

추가된 파일:
- `src/app/owner/requests/[id]/page.tsx` — 요청 상세 + 답변 + 상태 변경
- `src/app/owner/requests/[id]/page.module.css`

수정된 파일:
- `src/types/index.ts` — RequestVisibility 타입, EmployeeRequest에 visibility 추가, RequestReply 인터페이스 신설
- `src/mock/data.ts` — visibility 필드 추가, mockReplies 추가
- `src/app/owner/requests/page.tsx` — 기본 뷰 list로 변경, visibility 뱃지, 클릭 → detail 이동
- `src/app/owner/requests/page.module.css` — listTypeCell (유형+공개범위 2행), visibilityOwner/visibilityAll 스타일

상세 페이지 구조:
- 헤더: 뒤로가기 + 유형 + 상태 뱃지
- 본문(article): 유형 태그 + 공개범위 뱃지 + 내용 + 사진 placeholder + 작성자/일시
- 답변 섹션: 답변 목록(사장 뱃지 포함) + textarea 입력 + 등록 버튼
- 우측 사이드 패널: 상태 변경(2x2 그리드) + 요청 정보(요청자/일시/공개/사진)
- 데스크탑: 2-컬럼(mainPanel flex:1 / sidePanel 260px sticky)

## 18. 요청함 목록/상세 전면 재디자인

사용자 요청: 현재 디자인 별로, 재디자인.

**목록 페이지 재설계:**
- 리스트 뷰: 인박스 스타일 (Gmail 스타일). 그리드 3-컬럼 (유형뱃지 | 내용2줄+메타 | 상태+일시)
- REQUESTED(미확인) 행: 파란 좌측 border 강조, content bold
- 유형별 컬러 뱃지 (재료부족=오렌지, 장비고장=레드, 근무변경=블루 등)
- 카드 뷰: 좌측 4px colored border + 3줄 content preview

**상세 페이지 재설계:**
- 게시글 본문: 유형+공개범위 태그, 본문 텍스트, 아바타+작성자+일시 footer
- 답변: 채팅 말풍선 (사장=오른쪽+파란색, 직원=왼쪽+흰색)
- 입력창: 둥근 input (radius:20px) + 원형 전송 버튼. Enter=전송, Shift+Enter=줄바꿈
- 우측 패널: 상태 pill 버튼 + 요청 메타정보
- 데스크탑: mainPanel에 border+radius 카드화, 사이드패널 sticky 260px

## 19. 요청함 상세페이지 댓글+팝업+활동히스토리로 전환

사용자 요청: 채팅 말풍선 대신 댓글 형태. 상태변경은 팝업 모달. 우측에 활동 히스토리 타임라인.

수정된 파일:
- `src/types/index.ts` — ActivityType, ActivityLog 인터페이스 추가
- `src/mock/data.ts` — mockActivityLogs 배열 추가 (CREATED/STATUS_CHANGED/COMMENT_ADDED 항목)
- `src/app/owner/requests/[id]/page.tsx` — 전면 재작성 (댓글 UI, 상태 팝업, 활동 타임라인)
- `src/app/owner/requests/[id]/page.module.css` — 전면 재작성 (신규 클래스 전체)

주요 변경:
- 댓글: GitHub 스타일 (아바타 원 + 이름/뱃지/시간 헤더 + 본문). 사장 댓글에 "사장" 파란 뱃지
- 상태 변경: "상태 변경" 버튼 → 팝업 모달 오버레이. 4개 옵션 버튼, 현재 상태 색상 강조
- 활동 히스토리: 우측 사이드카드. 세로 타임라인, 점 색상으로 활동 유형 구분
  - CREATED: 파란 점 / STATUS_CHANGED: 주황 점 / COMMENT_ADDED: 초록 점 / CONTENT_EDITED: 회색 점
- submitReply(), changeStatus() 호출 시 activityLogs 상태에 새 ActivityLog 추가
- 데스크탑: mainPanel(flex:1) + sidePanel(280px sticky) 2-컬럼

주요 결정:
- 팝업 오버레이 클릭 시 닫힘 (e.stopPropagation으로 팝업 내부 클릭은 전파 차단)
- ActivityLog는 page.tsx 내 로컬 state로 관리 — 페이지 방문 시 mockActivityLogs에서 초기화
- 타임라인 연결선: ::after pseudo-element로 세로선 (z-index 0, 점이 z-index 1로 위에 표시)

## 20. 헤더 높이 통일 + 검색 기능 + 기타 개선

사용자 요청 모음: 헤더 높이 통일, 사이드바 매장 박스 높이 정렬, 검색 추가, 이모지 금지, 가입신청 별도 페이지 분리.

수정된 파일:
- `src/app/globals.css` — `--header-height: 70px` CSS 변수 추가
- `src/app/owner/requests/[id]/page.tsx` — 타임라인 최신순 정렬, 글 하단 작성자 정보 제거
- `src/app/owner/requests/[id]/page.module.css` — 전면 재작성 (댓글/팝업/타임라인 스타일)
- `src/app/owner/requests/page.tsx` — 유형 드롭다운 + 검색(버튼 클릭 시 적용) + 상태 필터 chip
- `src/app/owner/requests/page.module.css` — 헤더에 검색 영역 이동(태블릿+), toolBar 제거
- `src/app/owner/OwnerNav.tsx` — "루틴" 브랜드 텍스트 제거
- `src/app/owner/OwnerNav.module.css` — storeTrigger `height: var(--header-height)` 적용으로 구분선 위치 통일
- `src/app/owner/attendance/page.module.css` — header height 통일
- `src/app/owner/page.module.css` — header height 통일
- `src/app/owner/checklists/page.module.css` — header height 통일
- `src/app/owner/employees/page.tsx` — 검색 추가, 가입신청 탭 제거, 배너 추가
- `src/app/owner/employees/page.module.css` — 탭 스타일 제거, joinBanner 스타일 추가
- `src/app/owner/employees/[id]/page.module.css` — header height 통일

추가된 파일:
- `src/app/owner/employees/join-requests/page.tsx` — 가입신청 전용 페이지
- `src/app/owner/employees/join-requests/page.module.css`

주요 결정:
- `height: var(--header-height)` + `padding: 0 [lr]px` 패턴을 모든 .header 요소에 적용. padding: 16px 방식은 높이를 콘텐츠에 따라 변동시키므로 제거.
- storeTrigger에 `height: var(--header-height)` 적용으로 매장 선택 박스 하단 구분선이 헤더 하단 경계선과 정확히 일치.
- 요청함 헤더: 검색/유형 영역을 헤더 우측에 이동(태블릿+). toolBar sticky 제거. 상태 chip은 body 첫 줄로 이동. 모든 헤더 높이 통일됨.
- 가입신청 분리: 직원 관리 페이지 하단에 "가입 신청 N건 대기 중 →" 배너 표시 → `/owner/employees/join-requests` 로 이동. 배너는 mockJoinRequests.length > 0일 때만 표시.
- 이모지 완전 금지 — react-icons/lia 아이콘만 사용.

## 21. 페이지 헤더 좌측 여백 통일 + 필터 칩 스타일 통일 + 뱃지 가이드 + 세부검색 패널

사용자 요청 모음: 헤더 텍스트 시작 위치 통일, 필터 칩 스타일 통일, 뱃지 크기/위치 통일(디자인 에이전트 가이드), 세부 검색 패널.

수정된 파일:
- `src/app/globals.css` — `--page-x` 반응형 변수 추가 (16/24/32px)
- `src/app/owner/requests/page.tsx` — 전면 재작성: 세부검색 패널(내용/작성자/기간/유형/공개범위/상태), 필터 초기화, 뱃지 가이드 적용
- `src/app/owner/requests/page.module.css` — 전면 재작성: advPanel/advRow/advChips/chip/badge 스타일
- `src/app/owner/employees/page.tsx` — 가입신청 배너 위치 조정 (filterRow 하단), 필터 칩 스타일 통일
- `src/app/owner/employees/page.module.css` — 필터 칩 통일, filterRow 768px+ transparent
- `.claude/agents/design-agent.md` — 컴포넌트 스타일 가이드 섹션 추가 (필터 칩, 뱃지, 헤더 레이아웃)

가입신청 분리, 이전 세션 작업 포함.

주요 결정:
- `--page-x` CSS 변수: 16/24/32px 반응형 헤더 좌우 패딩 통일
- 필터 칩: `padding: 5px 13px; border-radius: 99px; font-size: 13px`, active = `--color-primary-light`
- 상태 뱃지: pill (border-radius: 99px, font-size: 12px, padding: 3px 9px)
- 유형/속성 뱃지: rect (border-radius: 6px, font-size: 11px, padding: 3px 8px)
- 세부검색 패널: sticky below header (top: var(--header-height)), 상태 칩 패널 내부로 이동
- 필터 초기화 버튼: 내용 검색 버튼 우측 배치
- 작성자 검색: 즉시 적용(onChange), 내용 검색: 버튼 클릭 적용

## 22. 세부검색 작성자 선택 팝업

사용자 요청: 작성자 인풋 옆 버튼 → 팝업으로 등록 직원 목록 표시, 선택 시 작성자 필드에 이름 입력.

수정된 파일:
- `src/app/owner/requests/page.tsx` — `authorPopupOpen` 상태 추가, 작성자 row에 LiaUsersSolid 버튼 + 팝업(직원 목록) 추가
- `src/app/owner/requests/page.module.css` — `.authorPickerWrap`, `.authorPickerBtn`, `.authorPickerOverlay`, `.authorPickerPopup`, `.authorPickerItem`, `.authorPickerName`, `.authorPickerPhone` 추가

동작:
- 버튼(LiaUsersSolid 아이콘) 클릭 → 드롭다운 팝업 (mockEmployees 전체 목록, 이름+전화번호)
- 직원 선택 → filterAuthor에 이름 설정, 팝업 닫힘
- 오버레이 클릭 → 팝업 닫힘 (fixed inset 투명 오버레이)

## 23. 작성자 선택 팝업 → 센터 모달 + 이름 검색

사용자 요청: 드롭다운 말고 팝업 형태. 팝업 내 이름 검색 기능.

수정된 파일:
- `src/app/owner/requests/page.tsx` — `empSearchText` 상태 추가, 팝업을 page root 레벨 모달로 변경 (advPanel 밖으로 이동), 이름 검색 필터링 추가
- `src/app/owner/requests/page.module.css` — `.authorPickerOverlay` fixed+flex+center, `.authorPickerPopup` 고정 너비 모달, `.authorPickerSearch` 검색 인풋, `.authorPickerList` 스크롤 영역

주요 결정:
- 팝업은 page 최상위에 조건부 렌더링 (advPanel 안에 있으면 advPanel overflow: hidden에 의해 잘릴 수 있음)
- 이름 검색은 empSearchText 별도 상태, 팝업 내부에서만 동작
- 직원 선택 시 empSearchText 초기화하여 다음 팝업 열 때 검색창 비움

## 24. 필터 초기화 버튼 높이 + 세부검색 패널 애니메이션

사용자 요청: 필터 초기화 버튼 높이를 검색 버튼과 동일하게. 세부검색 열고 닫을 때 애니메이션.

수정된 파일:
- `src/app/owner/requests/page.module.css` — `.advResetBtn` padding 동일화. `.advPanel` max-height/opacity/padding 전환 방식으로 변경
- `src/app/owner/requests/page.tsx` — `{advOpen && <div>...}` → 항상 렌더링 div로 변경 + `advPanelOpen` 클래스 토글

주요 결정:
- CSS transition 양방향 애니메이션: 조건부 렌더링이 아닌 항상 DOM에 존재, class 전환으로 처리
- `max-height: 0 → 600px`, `opacity: 0 → 1`, `padding: 0 → 14px` 3속성 동시 transition
- 빌드 에러(Expected ',' got '{'): 기존 `)}` 처리용 `</div>` 하나를 중복으로 남긴 것. 제거로 해결.

## 25. 모바일 세부검색 버튼 표시 문제 수정

사용자 요청: 모바일 화면에서 세부검색 버튼이 안 보임.

원인: 세부검색 버튼이 `.headerSearch` 안에 있었고, `.headerSearch { display: none }` (모바일 기본값)로 숨겨짐.

수정된 파일:
- `src/app/owner/requests/page.tsx` — 세부검색 버튼을 `.headerSearch` 밖 `.header` 직속 자식으로 이동
- `src/app/owner/requests/page.module.css` — `.advBtnLabel { display: none }` 모바일, 768px+에서 `display: inline` (아이콘만 → 아이콘+텍스트)

## 26. 직원관리 세부검색 패널 적용

사용자 요청: 요청함과 동일한 검색+세부검색 패턴을 직원관리에도 적용.

수정된 파일:
- `src/app/owner/employees/page.tsx` — `filterStatus` 상태를 'ALL' | EmploymentStatus로 변경, `filterDays`, `advOpen`, `advActiveCount`, `resetAdv()`, `toggleDay()` 추가. 기존 `filterRow` 제거, advPanel로 통합.
- `src/app/owner/employees/page.module.css` — `.advBtn`, `.advBtnActive`, `.advBtnLabel`, `.advPanel`, `.advPanelOpen`, `.advRow`, `.advLabel`, `.advSearchWrap`, `.advChips`, `.advResetBtn` 추가. `filterRow` 투명 override 제거.

세부검색 내용: 이름/전화 검색 + 상태(전체/재직중/퇴직) chip + 근무요일(월~일) chip.

## 27. 요청함 칸반 보드 (웹 환경 5-컬럼)

사용자 요청: 웹 환경에서 가로로 5개 컬럼 상태별 칸반 보드 표시.

수정된 파일:
- `src/app/owner/requests/page.tsx` — `KanbanCard` 컴포넌트 추가, `KANBAN_STATUSES` 상수, 칸반 뷰 JSX 추가
- `src/app/owner/requests/page.module.css` — `.kanban`, `.kanbanCol`, `.kanbanColHeader`, `.kanbanColTitle`, `.kanbanColCount`, `.kanbanColBody`, `.kanbanCard`, `.kanbanCardBadges`, `.kanbanCardContent`, `.kanbanCardMeta` 추가

주요 결정:
- 모바일(< 768px): 기존 카드/리스트 뷰 + viewToggle 유지
- 768px+: `.body { display: none }`, `.viewToggle { display: none }`, `.kanban { display: flex }` — 자동 전환
- 칸반 컬럼: `flex: 1; min-width: 180px; border-radius: 12px; border: 1px solid var(--color-border)` + `overflow-x: auto`로 컬럼 수평 스크롤

## 28. HTML5 드래그앤드롭으로 상태 변경

사용자 요청: 드래그앤드롭으로 칸반 카드 상태 변경.

수정된 파일:
- `src/app/owner/requests/page.tsx` — `draggingId`, `dragOverStatus` 상태 추가. `handleDragStart`, `handleDragEnd`, `handleDrop` 핸들러. KanbanCard에 `draggable` + drag 이벤트. 컬럼에 `onDragOver/Enter/Leave/Drop` 이벤트. `requests` 상태로 mock을 로컬 관리.
- `src/app/owner/requests/page.module.css` — `.kanbanColDragOver` (테두리 강조), `.kanbanCardDragging` (opacity 0.4, rotate(1deg))

주요 결정:
- `dragLeave` 오발: `e.currentTarget.contains(e.relatedTarget)` 체크로 자식 요소 진입 시 false clear 방지
- `kanbanCol`에서 `overflow: hidden` 제거 — 툴팁이 잘리는 문제 해결 (이후 툴팁 추가 시 대비)
- 칸반 컬럼 배경색(상태별 컬러)은 사용자 요청으로 제거 → 단색 `.kanbanColBody` 유지

## 29. 목업 데이터 30개로 확장 + DONE/REJECTED 30일 필터 + 툴팁

사용자 요청: 목업 데이터 10배 확장. 완료/반려 컬럼은 최근 30일만 표시, 마우스 오버 시 툴팁.

수정된 파일:
- `src/mock/data.ts` — `mockRequests` 3개 → 30개 (상태별 6개씩)
- `src/app/owner/requests/page.tsx` — `THIRTY_DAYS_AGO` 상수, colItems 필터에 30일 조건, DONE/REJECTED 헤더에 `LiaInfoCircleSolid` 아이콘 + `data-tooltip` 속성
- `src/app/owner/requests/page.module.css` — `.kanbanColTitleWrap`, `.kanbanColTitleWrap::after` (CSS-only 툴팁), `.kanbanColInfoIcon`

주요 결정:
- 툴팁: `::after { content: attr(data-tooltip) }` + `opacity: 0 → 1 on hover`. JS 없이 순수 CSS.
- `kanbanCol`에 `overflow: hidden` 없음 → 툴팁이 컬럼 위로 돌출 가능
- 원본 ID 1, 2, 3은 상세 페이지 링크 호환을 위해 유지

## 30. 요청 내용 2줄 말줄임

사용자 요청: 목록/칸반 카드에서 내용이 2줄 초과 시 `...` 처리.

수정된 파일:
- `src/app/owner/requests/page.module.css` — `.content` `-webkit-line-clamp: 3 → 2`, `.kanbanCardContent` `-webkit-line-clamp: 3 → 2`

`.listPreview`는 이미 `-webkit-line-clamp: 2`였으므로 변경 없음.

## 31. 요청 상세 — 요청자 프로필 팝업 + 관리 기능 (유형/공개범위 변경, 삭제)

사용자 요청: 요청 정보의 요청자 이름 클릭 시 직원 프로필 팝업. 사장은 유형 변경, 공개범위 변경, 삭제 가능.

수정된 파일:
- `src/app/owner/requests/[id]/page.tsx` — 전체 재작성
- `src/app/owner/requests/[id]/page.module.css` — 신규 클래스 추가

주요 변경:
- `type`, `visibility`를 로컬 state로 올려서 수정 반영 가능하게 변경 (기존: `request.type`, `request.visibility` 직접 참조)
- 요청자 이름 → `<button className={styles.metaAuthorBtn}>` — 클릭 시 직원 프로필 팝업
- 직원 프로필 팝업: mockEmployees에서 이름으로 직원 검색, 아바타/이름/상태뱃지/연락처/생년월일/입사일/근무요일dots/근무시간 표시. 직원 정보 없을 경우 "찾을 수 없습니다" fallback.
- "요청 관리" sideCard 추가: 유형 변경 버튼, 공개범위 변경 버튼, 요청 삭제 버튼(danger)
- 유형 변경 팝업: 6개 유형 목록, 현재 유형 강조 (popupOptionTypeActive)
- 공개범위 변경 팝업: 전체공개 / 사장만 2개 옵션
- 삭제 확인 팝업: 취소/삭제 버튼, 확인 시 router.back()
- 유형/공개범위 변경 시 활동 내역에 CONTENT_EDITED 로그 자동 추가

## 32. 전역 토스트 메세지 시스템

사용자 요청: 처리 시 상단에서 토스트 메세지가 뜨게. 모든 영역에서 범용으로 사용 가능하게.

추가된 파일:
- `src/context/ToastContext.tsx` — `ToastProvider`, `useToast`, `ToastType` export. 슬라이드 인/아웃 애니메이션 + 자동 소멸(2.8s) + 수동 닫기.
- `src/context/ToastContext.module.css` — `.container`, `.toast`, `.toastExiting`, `.toast_success/error/info/warning`, `.toastIcon`, `.toastMessage`, `.toastClose`

수정된 파일:
- `src/app/layout.tsx` — `<ToastProvider>` 루트 레이아웃에 주입
- `src/app/owner/requests/[id]/page.tsx` — `useToast` 연결. 상태 변경, 유형 변경, 공개범위 변경, 댓글 등록, 삭제 시 각각 토스트 표시

동작:
- `showToast(message, type?)` — type 기본값 `'success'`
- 타입: `success`(초록), `error`(빨강), `info`(파랑), `warning`(노랑)
- 여러 토스트 동시 표시 가능, 위에서 아래로 쌓임
- 삭제 시: 토스트 표시 후 800ms 뒤 `router.back()` (토스트를 먼저 보여주고 이동)

## 33. 전역 알림 벨 아이콘

사용자 요청: 모든 헤더에 고정 알림 벨 아이콘. 미읽음 수 뱃지. 누르면 알림 목록 드롭다운.

추가된 파일:
- `src/context/NotificationContext.tsx` — `NotificationProvider`, `useNotification`, `NotificationItem`, `NotificationType` export. 5개 목업 알림 (REQUEST x2, ATTENDANCE, CHECKLIST, SYSTEM).
- `src/components/NotificationBell.tsx` — `position: fixed; top: 0; right: var(--page-x); z-index: 20`. 벨 아이콘 + 미읽음 뱃지(빨간 원) + 드롭다운 패널.
- `src/components/NotificationBell.module.css`

수정된 파일:
- `src/app/layout.tsx` — `NotificationProvider` 주입 (ToastProvider 바깥에서 감쌈)
- `src/app/owner/layout.tsx` — `<NotificationBell />` 추가
- `src/app/employee/layout.tsx` — `<NotificationBell />` 추가

알림 패널 기능:
- 알림 타입별 컬러 아이콘 (REQUEST=파랑, ATTENDANCE=노랑, CHECKLIST=초록, SYSTEM=회색)
- 미읽음 아이템: `--color-primary-light` 배경 + 오른쪽 파란 점
- 아이템 클릭: 해당 알림 읽음 처리 + href 있으면 해당 페이지로 이동
- "모두 읽음" 버튼: 미읽음이 있을 때만 표시
- 패널 바깥 투명 오버레이 클릭 시 닫힘

신규 CSS 클래스:
- `.metaAuthorBtn` — 파란 underline 링크 스타일 버튼
- `.manageActions`, `.manageBtn`, `.manageBtnDanger` — 관리 버튼 그룹
- `.popupOptionTypeActive` — 유형/공개범위 팝업 현재 선택 상태
- `.deleteConfirmText`, `.deleteConfirmActions`, `.deleteCancelBtn`, `.deleteConfirmBtn` — 삭제 확인 팝업
- `.profileBody`, `.profileAvatar`, `.profileName`, `.profileStatusBadge`, `.profileBadgeActive/Inactive`, `.profileMeta*`, `.profileDowRow/Dot/DotOn`, `.profileNotFound` — 직원 프로필 팝업

## 34. 알림 벨 — AppBar 방식 폐기 → OwnerNav 사이드바 하단으로 이동

사용자 피드백: AppBar 방식(벨을 최상단 sticky bar에 배치, 페이지 헤더들이 그 아래로 밀리는 구조)이 "별로다". 대신 왼쪽 메뉴바 하단에 로그인 유저 이름 + 벨 표시를 원함.

수정된 파일:
- `src/app/globals.css` — `--appbar-height: var(--header-height)` → `--appbar-height: 0px` (페이지 헤더 sticky top 값 전부 자동 원복)
- `src/app/owner/StoreBar.tsx` — 벨 제거, 올바른 클래스명(.storeBtn/.storeName/.storeChevron) 사용
- `src/app/owner/StoreBar.module.css` — 태블릿+에서 `.bar { display: none }`. sticky/z-index 제거 (단순 모바일 전용 바)
- `src/app/owner/OwnerNav.tsx` — `NotificationBell` import 추가. `userSection` 블록(아바타+이름+벨) 추가 (nav 마지막 자식)
- `src/app/owner/OwnerNav.module.css` — `.userSection`/`.userAvatar`/`.userName` 반응형 스타일 추가. `overflow: hidden → visible` 변경. 모바일: 탭바 우측 벨 전용 영역(52px). 태블릿: 사이드바 하단 벨 아이콘 + `margin-top: auto`. 데스크탑: 아바타(30px 원)+이름 텍스트+벨 가로 배열.
- `src/components/NotificationBell.module.css` — 패널 위치 변경. 모바일: `bottom: calc(var(--nav-height)+8px); right: 4px`. 태블릿: `bottom: 72px; left: calc(var(--sidebar-sm)+8px)`. 데스크탑: `left: calc(var(--sidebar-lg)+8px)`. 애니메이션 방향 아래→위(translateY 8px → 0).
- `src/app/employee/layout.tsx` — `EmployeeAppBar` 제거, 원래 구조(`main + EmployeeNav`)로 복원
- `src/app/employee/AppBar.tsx` — 삭제 (미사용)
- `src/app/employee/AppBar.module.css` — 삭제 (미사용)

주요 결정:
- `--appbar-height: 0px`로 설정하면 이전에 변경한 페이지 헤더 CSS(`top: var(--appbar-height)` 등)가 자동으로 `top: 0`처럼 동작 — 개별 파일을 하나씩 되돌리지 않아도 됨.
- 태블릿: 사이드바는 overflow: visible로 변경해야 벨 뱃지가 잘리지 않음.
- 드롭다운 패널은 position: fixed라 overflow: hidden에 영향 없음 (뷰포트 기준 위치 계산).
- 현재 로그인 유저 이름은 프로토타입에서 "사장님" 하드코딩.

## 35. 모바일 하단 바 3등분 재설계 + 메뉴 드로어

사용자 요청: 모바일 하단 탭 5개 → 3등분 (왼쪽: 메뉴, 중앙: 알림, 오른쪽: 로그인 계정).

수정된 파일:
- `src/app/owner/OwnerNav.tsx` — 전면 재구성. `mobileMenuOpen` 상태 추가. `.nav` (사이드바)와 `.mobileBar` (3등분 바)를 분리. 드로어 렌더링 추가. `LiaBarsSolid`(메뉴), `LiaTimesSolid`(닫기) 아이콘 추가. `navItems`에서 `short` 프로퍼티 제거 (사이드바에서는 `fullLabel`만 사용).
- `src/app/owner/OwnerNav.module.css` — 전면 재작성. 모바일에서 `.nav { display: none }` + `.mobileBar { display: flex }` 패턴으로 분리. 태블릿+에서 반대. 드로어/오버레이/드로어아이템 스타일 추가.

구조:
- **모바일 `.mobileBar`**: 3개 flex 영역 (메뉴버튼 | 벨 | 유저버튼). 중앙 벨 영역 양쪽에 border-left/right 구분선.
- **드로어 (`.drawer`)**: `position: fixed; bottom: 0; z-index: 151`. 바텀시트 스타일 (border-radius 20px top). handle bar + 매장명/닫기 헤더 + nav 항목 리스트. `drawerUp` 키프레임 슬라이드 업 애니메이션. 활성 항목은 `--color-primary-light` 배경.
- **오버레이 (`.drawerOverlay`)**: `position: fixed; inset: 0; z-index: 150`. 탭 시 드로어 닫힘.
- **태블릿+ (768px+)**: `.mobileBar { display: none }`, `.nav { display: flex }` — 기존 아이콘 사이드바 유지.
- **데스크탑 (1024px+)**: 기존 아이콘+라벨 사이드바 유지.

주요 결정:
- `NotificationBell`은 `.userSection`(태블릿+)에서만 사용. 모바일 벨은 OwnerNav 내 직접 구현.

## 37. 모바일 하단 바 — 다이아몬드·알림 위치 교체 + 알림 전체화면

사용자 요청: 다이아몬드에 메뉴 아이콘을 넣고, 알림 버튼은 왼쪽으로. 모바일에서 알림 누르면 팝업 대신 전체화면으로.

수정된 파일:
- `src/app/owner/OwnerNav.tsx` — 전면 재작성. `notifOpen` 상태 추가. 왼쪽=알림, 중앙=메뉴다이아몬드, 오른쪽=계정 구조로 변경. 알림 전체화면 오버레이 인라인 구현. `useNotification`, `NotificationType`, `LiaBellSolid`, `LiaCheckSolid`, `LiaAngleLeftSolid`, `LiaInfoCircleSolid` 추가 임포트.
- `src/app/owner/OwnerNav.module.css` — `.mobileBellArea/.bellDiamond` → `.mobileDiamondArea/.menuDiamond`로 교체. `.menuDiamondIcon { transform: rotate(-45deg) }` (역회전). `.mobileBellWrap/.mobileBellBadge` 추가. `.notifScreen` 전체화면 오버레이 스타일 추가 (right 방향 slide-in 애니메이션). 알림 아이템 스타일(`.notifItem`, `.notifItemUnread`, `.notifIcon`, `.notifType_*`, `.notifBody` 등) 추가. `@media (min-width: 768px) { .notifScreen { display: none } }`.

구조 변화:
- **알림(왼쪽)**: `LiaBellSolid` 아이콘 + 미읽음 뱃지(빨간 원). 클릭 시 `notifOpen = true`.
- **메뉴(다이아몬드)**: `LiaBarsSolid` 아이콘, `transform: rotate(-45deg)`으로 수직 표시.
- **알림 전체화면**: `position: fixed; inset: 0; z-index: 200`. 헤더(뒤로가기 + 제목 + 모두읽음) + 알림 목록 (타입별 아이콘/컬러, 미읽음 강조, 읽음 클릭). 태블릿+에서는 `display: none`.

주요 결정:
- 모바일 알림은 `NotificationBell` 컴포넌트를 사용하지 않고 OwnerNav 내 직접 구현 — 전체화면 UX를 위한 별도 상태/렌더링 필요.
- 타입 아이콘 매핑(`TYPE_ICON`)을 OwnerNav.tsx에 재선언 (NotificationBell에서 미노출이므로 중복 허용).
- 알림 전체화면은 `translateX(24px) → 0` 슬라이드-인 애니메이션으로 네이티브 앱 페이지 전환 느낌.
- 드로어 닫힘 조건: 오버레이 클릭 OR 닫기 버튼 OR 메뉴 항목 선택.
- 유저 버튼 우측: 현재는 아바타+이름만 표시 (향후 계정 설정 연결 가능).

## 36. 모바일 벨 다이아몬드 디자인

사용자 요청: 메뉴/사장님 영역 넓히고, 종 모양을 다이아몬드 형태로 탭바 위로 올라오게.

수정된 파일:
- `src/components/NotificationBell.tsx` — `diamond?: boolean` prop 추가. `wrapDiamond` 클래스 조건부 적용.
- `src/components/NotificationBell.module.css` — `.wrapDiamond` (역회전 -45deg), `.wrapDiamond .bell` (화이트 컬러, padding 확대), `.wrapDiamond .badge` (위치 조정) 추가.
- `src/app/owner/OwnerNav.tsx` — `.mobileBell` → `.mobileBellArea` + `.bellDiamond` 래퍼로 변경. `<NotificationBell diamond />` 전달.
- `src/app/owner/OwnerNav.module.css` — `.mobileBellArea { flex: 0 0 72px; position: relative }`. `.bellDiamond { position: absolute; top: -20px; width/height: 52px; border-radius: 10px; transform: rotate(45deg); border: 3px solid surface; box-shadow }`. `.mobileBtn { flex: 1; padding: 0 8px }` (메뉴/유저 영역 확장).

동작 원리:
- 다이아몬드 컨테이너(`.bellDiamond`): `transform: rotate(45deg)` → 정사각형이 45° 회전하여 다이아몬드 모양. 흰색 테두리 + 파란 그림자로 탭바 위로 떠오르는 효과.
- 아이콘/벨(`.wrapDiamond`): `transform: rotate(-45deg)` → 부모의 45° 회전을 상쇄하여 벨 아이콘이 수직으로 표시됨.
- `position: absolute; top: -20px`으로 탭바 상단 기준 약 30px 위로 돌출.
- `border: 3px solid var(--color-surface)` (흰 테두리)로 페이지 콘텐츠 위에 떠 있는 느낌.

## 38. 모바일 메뉴 버튼 다이아몬드 → 원형 전환 + 크기/위치 조정

사용자 요청 (순서): ① 다이아몬드 대신 동그라미로 변경 ② 원 크게 ③ 위치 조금 위로

수정된 파일:
- `src/app/owner/OwnerNav.module.css` — `.menuDiamond`에서 `transform: rotate(45deg)` 제거 → `border-radius: 50%`로 교체. `width/height 56px`로 확대. `top: -2px`으로 미세 위치 상향.
- 아이콘 역회전 (`transform: rotate(-45deg)`)도 함께 제거 — 정방향 `LiaBarsSolid` 표시.

주요 결정:
- 다이아몬드는 rotate(45deg) + 내부 역회전(-45deg) 조합이었으나 동그라미로 바꾸면 두 transform 모두 불필요.
- `border: 3px solid var(--color-surface)` + `box-shadow` 유지 — 떠오르는 입체감 유지.

## 39. 모바일 하단 바 항상 표시 (z-index 스태킹 정리)

사용자 요청: 모바일 환경에서 하단 바가 항상 보여야 해.

수정된 파일:
- `src/app/owner/OwnerNav.module.css`
  - `.mobileBar { z-index: 201 }` — 모든 오버레이 위에 위치
  - `.notifScreen { bottom: var(--nav-height) }` — `inset: 0` 대신 탭바 높이만큼 남겨 탭바를 덮지 않음
  - `.drawerOverlay { bottom: var(--nav-height) }` — 동일

주요 결정:
- z-index 레이어: `mobileBar(201) > notifScreen(200) > drawer(151) > drawerOverlay(150)`
- 알림 전체화면·드로어 오버레이를 `inset: 0`에서 `bottom: var(--nav-height)`으로 변경 — 탭바가 시각적으로 항상 노출됨.

## 40. 월간 캘린더 출근/근무/퇴근 3분할 표시

사용자 요청: 월간 캘린더 날짜 칸에 단순 "N명" 대신 출근/근무/퇴근 각각 몇 명인지 표시.

수정된 파일:
- `src/app/owner/attendance/page.tsx` — 캘린더 셀 내 카운트 계산 및 렌더링 교체
- `src/app/owner/attendance/page.module.css` — 새 stat 클래스 추가, 셀 min-height 증가

변경 내용:
- **기존**: `{records.length}명` 단일 스팬
- **변경**: 3개 스팬(`출/근/퇴`) → 0명인 항목은 숨김
- **상태 매핑**:
  - 출근 (출): `CLOCKED_IN + LATE + CLOCKED_OUT` — 오늘 실제로 출근한 총 인원
  - 근무 (근): `CLOCKED_IN + LATE` — 현재 근무 중인 인원
  - 퇴근 (퇴): `CLOCKED_OUT` — 퇴근 완료 인원
- **CSS**: `.calStats` (flex-column), `.statIn`(파랑), `.statWork`(초록), `.statOut`(회색)
- **셀 높이**: 모바일 72→88px, 태블릿 92→108px, 데스크탑 112→128px

## 41. 주간 뷰 가로 7열 그리드로 재설계

사용자 요청: 주간 뷰를 세로 리스트가 아닌 가로 7열 그리드로.

수정된 파일:
- `src/app/owner/attendance/page.tsx` — 주간 뷰 JSX 전체 교체. 기존 7행 세로 버튼 목록 → `.weekGrid`(7열) + calHead 헤더 + `.weekCell` 7개.
- `src/app/owner/attendance/page.module.css` — 기존 주간 세로 목록 클래스 제거, 새 그리드 클래스 추가.

주요 결정:
- `.weekGrid`: `grid-template-columns: repeat(7, 1fr)` — 월간 달력과 동일한 구조
- 헤더 행: 월간 달력의 `.calHead` 재사용
- 셀 내 통계: `.calStats/.statIn/.statWork/.statOut` 재사용 (출/근/퇴 3분할)
- 오늘 날짜 강조, 선택 상태, alert dot 모두 월간과 동일하게 구현
- 셀 높이: 모바일 140px / 태블릿 200px / 데스크탑 240px (한 줄만 있어서 더 넉넉하게)
- 기존 `.weekRow/.weekNames/.weekCount` 등 CSS 클래스는 이번 변경으로 미사용 상태 (제거됨)

## 42. 날짜 상세 — Gantt 타임라인으로 교체

사용자 요청: 날짜 클릭 상세 시트를 그래프 형태 타임라인으로 변경.

수정된 파일:
- `src/app/owner/attendance/page.tsx` — `sheetBody` 내용 교체. 기존 레코드 목록 → Gantt 타임라인 (IIFE 방식으로 인라인 연산)
- `src/app/owner/attendance/page.module.css` — `.tlWrap`, `.tlAxisRow`, `.tlRow`, `.tlLabelCol`, `.tlTrackCol`, `.tlHourMark`, `.tlGridLine`, `.tlBarSched`, `.tlBarSchedAbsent`, `.tlBarActual`, `.tlBarActual_*`, `.tlBarOngoing`, `.tlEmpName`, `.tlEmpTime` 클래스 추가

구조:
- 상단 축 행: 시간 레이블 (8, 9, 10 ... 시)
- 직원별 행: 예정 시간 bar(회색 테두리) + 실제 근무 bar(컬러) 겹침 표시
- 라벨 열: `position: sticky; left: 0` — 가로 스크롤 시 이름/상태/시간 고정
- 시간 범위 자동 계산: min(all times) ~ max(all times)를 1시간 단위로 정렬

상태별 색:
- CLOCKED_OUT: 초록 (퇴근 완료)
- CLOCKED_IN: 파랑 + 오른쪽 페이드 (근무 중, 퇴근 시간 미확정)
- LATE: 주황 + 오른쪽 페이드 (지각 근무 중)
- ABSENT: 예정 bar만 표시 (사선 패턴)
- SCHEDULED: 예정 bar만 표시 (아직 미출근)

주요 결정:
- 수평 스크롤: 시간 범위가 넓을 때(이지은 22:00 퇴근 등) `overflow-x: auto`로 자연스럽게 처리
- 진행 중 페이드: `::after { gradient to surface color }` — 퇴근 시간 미확정임을 시각적으로 표현
- 태블릿 모달: width 460px → 560px으로 확대 (Gantt 차트에 더 넓은 공간 확보)

## 43. 달력 요일 헤더 색상 구분

사용자 요청: 요일 표시에 명암(색) 구분. 토=파랑, 일=빨강.

수정된 파일:
- `src/app/owner/attendance/page.tsx` — 월간 및 주간 `.calHead` 렌더링에 `i === 5 ? styles.calHeadSat : i === 6 ? styles.calHeadSun : ''` 조건부 클래스 추가 (`replace_all: true` 적용)
- `src/app/owner/attendance/page.module.css` — `.calHeadSat { color: var(--color-primary) }`, `.calHeadSun { color: var(--color-danger) }` 추가

주요 결정:
- `WEEKDAY_LABELS` 배열 인덱스 기준: 5=토, 6=일 (월~일 순서)
- 색상은 CSS 변수 사용 — 다크모드 등 테마 변경 시 자동 대응

## 44. 날짜/주간/월간 기간 레이블 클릭 → 날짜 픽커

사용자 요청: 일간/주간/월간 헤더의 기간 표기를 클릭하면 달력 형태 픽커가 나와서 원하는 날짜로 이동.
- 일간: 특정 날짜 선택
- 주간: 날짜 선택 시 해당 주 표시
- 월간: 월 선택

수정된 파일:
- `src/app/owner/attendance/page.tsx`
  - `LiaAngleDownSolid` import 추가
  - `pickerOpen`, `pickerNavYear`, `pickerNavMonth` state + `openPicker()` 함수 추가
  - `selectedWeekSet` (주간 픽커 강조용 Set) 계산 추가
  - 기간 레이블 영역을 `<div className={styles.periodNavWrap}>`으로 감싸고 픽커 팝업 추가
  - 월간: 연도 네비게이션 + 3열 12개월 그리드
  - 일간/주간: 월 네비게이션 + 7열 날짜 그리드 (주간은 선택된 주 7일 모두 강조)
- `src/app/owner/attendance/page.module.css`
  - `.periodNavWrap`, `.periodLabelBtn`, `.periodLabelIcon`, `.periodLabelIconOpen` — 레이블 버튼
  - `.pickerOverlay` — fixed inset 투명 오버레이 (외부 클릭 닫힘, z-index 49)
  - `.picker` — absolute 드롭다운 패널 (center-aligned, z-index 50, 14px 패딩, pickerIn 애니메이션)
  - `.pickerHead`, `.pickerNavBtn`, `.pickerTitle` — 픽커 내 네비게이션 헤더
  - `.pickerDayGrid` — 7열 날짜 그리드
  - `.pickerDow`, `.pickerDowSat`, `.pickerDowSun` — 요일 헤더 (토=파랑, 일=빨강)
  - `.pickerDay`, `.pickerDayToday`, `.pickerDaySelected` — 날짜 셀 (원형, 선택=파란 채움)
  - `.pickerDaySat`, `.pickerDaySun` — 토/일 날짜 색상
  - `.pickerMonthGrid` — 3열 월 그리드
  - `.pickerMonth`, `.pickerMonthSelected` — 월 버튼

주요 결정:
- 픽커 자체 네비게이션 상태(`pickerNavYear/Month`)와 메인 뷰 상태(`baseYear/Month`) 분리 — 픽커 내 월 이동이 메인 뷰에 영향을 주지 않음
- 주간 픽커: `selectedWeekSet`으로 7일 날짜 문자열 Set 생성, 픽커 날짜 셀 렌더 시 `.pickerDaySelected` 적용
- 픽커 오픈 시 `pickerNavYear/Month`를 현재 `baseYear/baseMonth`로 초기화
- `pickerIn` 키프레임: `translateY(-6px) opacity:0 → 0 opacity:1` 드롭다운 진입 애니메이션

## 45. 일간 뷰 — Gantt 타임라인으로 교체 + 타임라인 컴포넌트 추출

사용자 요청: 일간 달력에서는 그냥 타임라인을 보여주면 될 것 같음.

수정된 파일:
- `src/app/owner/attendance/page.tsx`
  - 시트 안에 IIFE로 인라인되어 있던 Gantt 타임라인 로직을 `AttendanceTimeline({ records })` 컴포넌트로 추출
  - 일간 뷰: 기존 직원 카드 목록(`.empCard`) → `<AttendanceTimeline records={dayRecords} />`
  - 날짜 상세 시트: IIFE → `<AttendanceTimeline records={selectedRecords} />` (동일 컴포넌트 재사용)
  - `import type { CalendarRecord }` 추가
- `src/app/owner/attendance/page.module.css`
  - `.dayWrap`을 surface 카드 스타일로 변경 (margin/border/radius/shadow) — 타임라인이 회색 배경 위에 떠 보이지 않도록
  - 일간 카드 전용 클래스 제거: `.empCard*`, `.empTop`, `.empName`, `.empTimes`, `.timeRow`, `.timeLabel`, `.timeAlert` (일간 카드 뷰 교체로 orphan됨)

주요 결정:
- 일간 뷰와 시트가 같은 `AttendanceTimeline` 컴포넌트를 공유 — 중복 제거, 단일 소스
- 빈 데이터는 컴포넌트 내부에서 "근무 데이터가 없습니다" 처리
- 미사용 dead code: `.record*` 클래스(시트가 Gantt로 바뀐 task 42부터 이미 미사용)는 이번 변경과 무관하여 그대로 둠

## 46. 주간 뷰 — 날짜별 7열 깃 그래프 타임라인

사용자 요청 (반복 피드백으로 형태 확정):
1. 주간도 타임라인을 깃 그래프처럼 보여달라
2. 면(그리드) 형태 말고, 깃 그래프처럼 한 줄기로 쭉 가면서 이벤트마다 점을 찍고 무슨 이벤트인지/몇 시인지 표기
3. 이걸 날짜별로 나눠 가로로 7개 컬럼으로

수정된 파일:
- `src/app/owner/attendance/page.tsx`
  - `buildDayEvents(records)` 헬퍼 추가 — `CalendarRecord[]`를 시간순 이벤트(`TimelineEvent[]`)로 변환
    - 출근(`CLOCKED_IN`)/지각 출근(`LATE`)/퇴근(`CLOCKED_OUT`)/출근 예정(`SCHEDULED`)/결근(`ABSENT`) 이벤트 생성
    - 각 이벤트: `{ time, type, label: "이름 + 동작" }`, `sortMin` 기준 정렬
  - `WeeklyTimeline` 컴포넌트: 7개 날짜 컬럼, 각 컬럼에 날짜 헤더 + 세로 깃 스트랜드(이벤트 점 + 라벨 + 시간)
  - `import type { AttendanceStatus }` 추가
- `src/app/owner/attendance/page.module.css`
  - 기존 주간 카운트 그리드(`.weekGrid`/`.weekCell*`) 제거 (task 41 추가분, 이번에 교체)
  - `.gitWeek` — 가로 7열 flex 컨테이너, `overflow-x: auto` (좁은 화면 가로 스크롤)
  - `.gitCol` — `flex: 1 0 124px`, 컬럼 구분 border-left
  - `.gitColHead` — 날짜 헤더(요일+날짜), 클릭 시 상세 시트, 토/일 색상, 오늘 강조
  - `.gitColBody::before` — 한 줄기 세로선, `.gitLane`(width 30, 중앙 정렬)로 점 중심을 선에 정렬
  - `.gitDot` + `.gitDot_*` — 이벤트 유형별 점 색상 (출근=파랑, 퇴근=초록, 지각=주황, 결근=빨강, 예정=회색), 흰 테두리 링으로 선 마스킹
  - `.gitEventBody` — 라벨(위) + 시간(아래) 세로 스택
  - `.gitColBodyEmpty` — 근무 없는 날: 세로선 숨김 + "—" 중앙 표시

주요 결정:
- 형태 변천: ① 7열 세로 막대(캘린더 그리드) → ② 한 줄기 세로 활동 로그 → ③ 날짜별 7열 깃 그래프(최종). 1·2단계 CSS/JSX는 최종본으로 교체됨.
- 점 중심 정렬: `.gitLane` 고정폭 + dot `box-sizing: content-box` + 흰 테두리로 세로선 위 깃 노드 효과
- 컬럼 클릭 → `selectedDate` 토글 → 주간 뷰에서 상세 Gantt 시트 드릴다운(기존 동작 유지)
- 좁은 화면: `min-width: 124px` × 7열은 가로 스크롤(일간 Gantt와 동일 패턴)

## 47. 날짜 상세 시트 — 웹에서 타임라인 크기 확대

사용자 요청: 날짜 클릭 시 뜨는 타임라인이 모바일은 적당한데 웹에서 작게 느껴짐. 웹에서 크기 확대.

수정된 파일:
- `src/app/owner/attendance/page.module.css`

변경 내용 (모바일은 그대로, 태블릿/데스크탑만 확대):
- 모달 폭: 태블릿(768px+) `560px → 680px`, 데스크탑(1024px+) `820px`, `max-height` 72vh→78vh
- 타임라인 행: `.tlRow .tlTrackCol height` 32px → 46px(768) → 56px(1024)
- 막대 두께: `.tlBarSched/.tlBarActual` top/bottom inset 6px → 9px(768) → 11px(1024)
- 행 패딩/축 높이/라벨열 폭/폰트도 단계적으로 상향 (`.tlLabelCol` 104→120px, `.tlEmpName` 14→15px 등)

주요 결정:
- 가로(모달 폭)와 세로(행 높이·막대 두께)를 함께 키워 Gantt가 웹에서 충분한 존재감을 갖도록 함
- 모바일 기본값은 변경하지 않음 — 사용자가 모바일은 적당하다고 함

## 48. 사장 메뉴 — 대 카테고리 그룹화

사용자 요청: 메뉴바를 대 카테고리로 나눔.
- 업무: 체크리스트, 요청함
- 스케줄: 현황
- 관리: 직원관리

수정된 파일:
- `src/app/owner/OwnerNav.tsx`
  - `navItems` 단일 배열 → `homeItem`(단독) + `navGroups`(카테고리별 그룹) 구조로 변경
  - 사이드바·드로어 양쪽 렌더링을 그룹 단위로 교체 (각 그룹: 카테고리 라벨 + 항목들)
  - `<homeItem.icon />` 멤버 표현식으로 홈 아이콘 렌더
- `src/app/owner/OwnerNav.module.css`
  - `.drawerGroupLabel` — 모바일 드로어 카테고리 라벨
  - 태블릿(768px+): `.navGroup` 그룹 간 구분선(border-top), `.navGroupLabel { display: none }` (아이콘 레일에서는 라벨 숨김)
  - 데스크탑(1024px+): `.navGroupLabel` 표시(작은 섹션 헤더), `.navGroup` 구분선 제거

주요 결정 / 가정:
- **홈**은 카테고리에 속하지 않은 최상단 단독 항목으로 유지
- 출근현황 라벨을 스케줄 카테고리 맥락에 맞춰 **"출근현황 → 현황"** 으로 변경
- 반응형 카테고리 표현: 항목 라벨이 보이는 곳(데스크탑 사이드바·모바일 드로어)에서는 카테고리 라벨 표시, 아이콘만 보이는 태블릿 레일에서는 구분선으로만 그룹 구분 (라벨/아이콘 표시 일관성)
- 직원(EmployeeNav) 메뉴는 대상 아님 — 사장 메뉴만 변경

## 49. "체크리스트" → "업무리스트" 명칭 변경

사용자 요청: 체크리스트라는 이름을 업무리스트로 변경.

수정된 파일 (사용자에게 보이는 UI 텍스트만, `체크리스트`→`업무리스트` 일괄 치환):
- `src/app/owner/OwnerNav.tsx` — 메뉴 라벨
- `src/app/owner/checklists/page.tsx` — 페이지 헤딩
- `src/app/owner/page.tsx` — 대시보드 "오픈/마감 업무리스트" 라벨
- `src/mock/data.ts` — 목 데이터 제목("오픈/마감/청소 업무리스트")
- `src/context/NotificationContext.tsx` — 알림 제목/본문
- `src/app/page.tsx` — 역할 선택 설명 문구
- `src/app/employee/checklist/[id]/page.tsx` — "업무리스트를 찾을 수 없습니다"

주요 결정:
- **코드 식별자는 변경하지 않음** — 라우트/폴더 `/owner/checklists`, 컴포넌트 `OwnerChecklists`, `mockChecklists`, 타입 `Checklist`, CSS 클래스 등은 그대로 유지(표시 명칭만 변경)
- `.claude/*` 문서·기획 MD는 UI가 아니므로 변경 대상에서 제외

## 50. 업무리스트 페이지 — 공통/추가 업무 + 직원 드래그드랍 배정

사용자 요청: 오픈/마감 구분 제거. 날짜별 업무리스트(공통 업무 리스트 / 추가 업무 리스트). 왼쪽에 현재 매장 직원 목록 + 오늘 출근 여부. 직원을 업무에 드래그드랍으로 배정.

확인된 결정(AskUserQuestion):
- 드래그 방향: **직원 → 업무 항목에 드롭** (그 직원에게 배정)
- 공통/추가 둘 다 직원 배정 가능

추가된 파일:
- `src/mock/tasks.ts` — `StoreTask`(id/title/completionType/kind/assigneeIds), `TaskKind`('COMMON'|'EXTRA'), `createTasksForDate(date)` (프로토타입: 날짜별 동일 템플릿 + 데모 기본 배정)

수정된 파일:
- `src/app/owner/checklists/page.tsx` — 전면 재작성 (`'use client'`)
  - 헤더에 날짜 네비게이션(‹ 6월 30일 (월) ›), 기본 오늘
  - 좌측 직원 패널: 재직 직원 목록 + 선택 날짜 출근 상태 뱃지(출근/지각/퇴근/예정/결근/휴무), `draggable` 드래그 소스
  - 우측: 공통 업무 리스트 + 추가 업무 리스트 섹션, 각 업무가 드롭 타겟
  - 직원을 업무로 드롭 → `assigneeIds`에 추가, 배정 칩(아바타+이름+× 해제) 표시
  - 상태: `tasksByDate`(날짜별 업무 맵, 지연 초기화), `draggingEmpId`, `dragOverTaskId`
  - 출근 상태는 `getAttendanceForDate(selectedDate)`와 직원 ID 매칭 (emp1~4 일치)
- `src/app/owner/checklists/page.module.css` — 전면 재작성 (날짜 네비/직원 패널/업무 섹션/배정 칩/드래그 하이라이트, 반응형 2단)

주요 결정 / 가정:
- 기존 `mockChecklists`(직원 수행 페이지 `/employee/checklist/[id]` 등이 사용)는 **건드리지 않고** 새 `mock/tasks.ts` 추가 — 다른 페이지 깨짐 방지
- 출근 상태 기준 날짜 = 선택된 날짜(기본 오늘) — "오늘 출근 여부"를 날짜 네비와 연동
- 날짜별 업무는 프로토타입상 동일 템플릿에서 시작, 세션 내 배정은 `tasksByDate` 맵에 날짜별로 유지
- 드래그드랍은 HTML5 DnD(데스크탑/마우스 기준) — 모바일은 레이아웃(직원 패널 가로 스크롤)만 우선 대응
- 빌드(`next build`) 성공 확인

## 51. 업무리스트 — 공통/추가 섹션 가로 배치

사용자 요청: 가로배치로.

수정된 파일:
- `src/app/owner/checklists/page.module.css` — 태블릿+(768px) `.taskArea`를 `flex-direction: row`로, `.taskSection { flex: 1 }`

결과: 태블릿/데스크탑에서 직원 패널 | 공통 업무 | 추가 업무 3열 가로 보드 형태. 모바일은 기존대로 세로 스택 유지.

## 52. 업무리스트 — 맨 오른쪽 테스크 목록(카탈로그) + 드래그로 리스트 추가

사용자 요청: 맨 오른쪽에 테스크 목록(이때까지 만든 테스크들). 이것도 드래그드랍으로 리스트에 추가 가능.

수정된 파일:
- `src/mock/tasks.ts`
  - 마스터 카탈로그 `TASK_CATALOG`(TaskTemplate[], 12개) 추가 — "이때까지 만든 모든 테스크"
  - `StoreTask`에 `catalogId`(원본 테스크 id) 추가, 인스턴스 id = `${kind}-${catalogId}`
  - `makeTask(catalogId, kind)` 헬퍼, `createTasksForDate`는 기본 공통/추가 구성에서 생성
- `src/app/owner/checklists/page.tsx`
  - 드래그 상태를 `dragItem: { type: 'EMP' | 'CATALOG'; id }`로 통합 (종류 구분)
  - 맨 오른쪽 `catalogPanel` 추가 — `TASK_CATALOG` 항목을 draggable로 렌더
  - 섹션(공통/추가)을 CATALOG 드롭 타겟으로: 드롭 시 `addTaskToList(kind, catalogId)` (같은 리스트 내 중복 catalogId 방지)
  - 업무 항목은 EMP 드롭 타겟(배정) 유지 — 중첩 드롭 타겟, onDrop에서 `dragItem.type`으로 분기 (stopPropagation 없이 타입 체크로 충돌 방지)
  - 업무 제거 버튼(`removeTask`, hover 시 ×) 추가 — 추가의 짝 기능
- `src/app/owner/checklists/page.module.css` — 전면 재작성: 사이드 패널 공통화(`.empPanel, .catalogPanel`), 카탈로그 아이템, `.taskSectionDragOver`, `.taskRemove`, 4열 가로 보드 레이아웃

주요 결정:
- 두 가지 드래그: **직원→업무(배정)**, **카탈로그 테스크→섹션(리스트 추가)**. `dragItem.type`으로 드롭 동작 분기, 중첩 타겟은 타입 불일치 시 no-op로 처리해 버블링 충돌 방지
- 카탈로그는 팔레트 개념 — 드롭해도 카탈로그에 남고, 리스트에 같은 테스크 중복 추가만 방지
- 새로 추가한 업무는 배정 없음(`assigneeIds: []`), 기본 템플릿 업무만 데모 배정(t1→김민수, t6→박서연)
- 레이아웃: 768+에서 직원 | 공통 | 추가 | 테스크목록 4열 가로 보드, 모바일은 세로 스택(패널은 가로 스크롤)
- 빌드(`next build`) 성공 확인

## 53. 업무리스트 — 전체 폭 사용 + 업무 영역 가로 확장

사용자 요청: 배열을 화면에 꽉 차게, 업무 리스트 부분을 가로로 길게.

수정된 파일:
- `src/app/owner/checklists/page.module.css` — `.layout`의 `max-width`(1280/1360px)·`margin: 0 auto` 제거 (768/1024 미디어 쿼리)

결과: 레이아웃이 전체 폭을 사용. 사이드 패널(직원/테스크목록)은 고정폭이라 늘어난 폭이 전부 업무 영역(`.taskArea` flex:1)으로 가서 공통/추가 섹션이 가로로 넓어짐.

## 54. 업무리스트 — 다중 배정 "n명 배정" + 호버 툴팁(직원 제거)

사용자 요청: 직원이 여러 명 배정되면 "n명 배정"으로 표시, 마우스 호버 시 툴팁, 툴팁에서 배정 직원 제거 가능.

수정된 파일:
- `src/app/owner/checklists/page.tsx` — 배정 표시를 3분기로:
  - 0명: "+ 직원 배정" 점선 힌트
  - 1명: 기존 이름 칩(× 해제)
  - 2명+: "n명 배정" 배지 + 호버 툴팁(직원 행마다 아바타·이름·× 제거)
- `src/app/owner/checklists/page.module.css`
  - `.assigneeMore`(relative) + `.assigneeCount` 배지 + `.assigneeTooltip`(absolute, top:100%) + `.assigneeTooltipCard` + `.tooltipRow/Avatar/Name/Remove`
  - `.taskSection { overflow: hidden → visible }` — 툴팁이 섹션 밖으로 나올 수 있게 (행/헤더가 투명 배경이라 모서리 라운드는 유지)
- `src/mock/tasks.ts` — 데모용 2명 배정 추가(t2 → emp1, emp2)로 툴팁 즉시 확인 가능

주요 결정:
- 툴팁은 CSS 호버(`.assigneeMore:hover .assigneeTooltip`) + `padding-top` 투명 브릿지로 배지→툴팁 이동 시 호버 연속성 확보(툴팁 내부 × 클릭 가능)
- 1명은 이름 칩 유지(누구인지 바로 보임), 2명+만 카운트로 접음 — 사용자 요청("여러명")에 맞춤
- 툴팁에서 제거해 1명/0명이 되면 자동으로 칩/힌트 표시로 전환
- 빌드(`next build`) 성공 확인

## 55. 테스크 목록 — 검색 추가

사용자 요청: 테스크 목록에 검색 추가.

수정된 파일:
- `src/app/owner/checklists/page.tsx` — `catalogQuery` 상태 + 카탈로그 패널에 검색 입력(아이콘+input+지우기 버튼). `filteredCatalog`로 제목 부분일치(대소문자 무시) 필터, 결과 없으면 "검색 결과가 없습니다"
- `src/app/owner/checklists/page.module.css` — `.catalogSearch`/`.catalogSearchIcon`/`.catalogSearchInput`/`.catalogSearchClear`/`.catalogEmpty`

주요 결정:
- 즉시 필터(onChange) — 별도 검색 버튼 없이 입력 즉시 반영
- 검색어 있을 때만 × 지우기 버튼 표시
- 빌드(`next build`) 성공 확인

## 56. 업무리스트 — 제목 설명을 정보 아이콘 호버 툴팁으로

사용자 요청: 직원/테스크 패널의 설명을 툴팁으로 변경. 공통/추가 업무 리스트에도 설명 툴팁 추가.
- 공통 업무: 주기로 등록해둔 업무들의 목록
- 추가 업무: 추가적으로 할당하는 리스트

수정된 파일:
- `src/app/owner/checklists/page.tsx`
  - `InfoTip({ text, align })` 컴포넌트 추가 (정보 아이콘 `LiaInfoCircleSolid` + 호버 툴팁)
  - 직원 패널: 항상 보이던 `.panelHint` 문구 제거 → 제목 옆 InfoTip("직원을 업무로 끌어다 놓아 배정합니다")
  - 테스크 목록 패널: `.panelHint` 제거 → 제목 옆 InfoTip(우측 정렬, "테스크를 …끌어다 놓아 추가합니다")
  - `renderSection`에 `desc` 인자 추가 → 공통/추가 섹션 제목 옆 InfoTip
- `src/app/owner/checklists/page.module.css`
  - `.panelTitle` flex(아이콘 인접), margin-bottom 12px / `.panelHint` 제거
  - `.infoTip`/`.infoIcon`/`.infoTooltip`/`.infoTooltipRight` 추가 (다크 툴팁, 호버 표시, pointer-events:none)

주요 결정:
- 설명을 상시 노출 대신 정보 아이콘 호버 툴팁으로 — 패널이 깔끔해지고 4곳 일관 적용
- 툴팁 방향: 기본 좌측 정렬(오른쪽으로 펼침), 최우측 테스크 목록 패널만 `align="right"`로 좌측 펼침 → 화면 밖 클리핑 방지
- 빌드(`next build`) 성공 확인

## 57. 업무리스트 — 섹션별 검색(테스크·직원) + 직원 패널 검색

사용자 요청: 업무리스트마다 검색(테스크 이름 + 할당된 직원 이름). 직원 패널도 검색.

수정된 파일:
- `src/app/owner/checklists/page.tsx`
  - `taskQuery: Record<TaskKind, string>` 상태 — `renderSection`에서 섹션별 검색 입력(헤더 아래) 추가, 테스크 제목 OR 배정 직원 이름 부분일치 필터, `sectionCount`는 필터 결과 수, 검색 중 빈 결과는 "검색 결과가 없습니다"
  - `empQuery` 상태 + `filteredEmployees` — 직원 패널에 검색 입력, 이름 필터
  - 카탈로그/직원 패널 검색 클래스 공용화: `catalogSearch*`→`panelSearch*`, `catalogEmpty`→`panelEmpty`
- `src/app/owner/checklists/page.module.css`
  - `.taskSearch`/`.taskSearchIcon`/`.taskSearchInput`/`.taskSearchClear` (섹션 내 검색바, border-bottom)
  - `catalogSearch*`→`panelSearch*`, `catalogEmpty`→`panelEmpty` 리네임(직원·테스크 패널 공용)

주요 결정:
- 섹션 검색은 테스크명과 배정 직원명을 모두 매칭(`title.includes || assigneeIds.some(name.includes)`)
- 드롭은 필터와 무관하게 전체 리스트에 추가 — 검색 중 추가한 테스크는 검색 해제 시 노출
- 사이드 패널 검색 스타일은 `panelSearch*`로 직원/테스크 목록 공용, 섹션 내부 검색은 `taskSearch*`로 분리
- 빌드(`next build`) 성공 확인

## 58. 업무리스트 — 직원별 할당 수 + 날짜 테스크 요약 + 완료 토글

사용자 요청: 직원 목록에 각 직원이 할당된 테스크 수 표시. 헤더 밑에 현재 날짜 테스크 요약(전체/할당/미할당/완료/미완료 등).

수정된 파일:
- `src/mock/tasks.ts` — `StoreTask`에 `done: boolean` 추가, `makeTask`에서 `DEFAULT_DONE`(t1/t3/t6) 기본 완료, 데모 데이터
- `src/app/owner/checklists/page.tsx`
  - `assignedCountByEmp` 계산 → 직원 카드에 "할당 N개"(이름 아래 메타) 표시 (`.empInfo` 컬럼 구조)
  - `summary` 계산(전체/할당/미할당/완료/미완료) → 헤더 아래 요약 바
  - `toggleDone(taskId)` + 각 테스크 행 왼쪽 완료 체크박스(원형, 완료 시 초록 체크), 완료 시 제목 취소선
  - `addTaskToList`로 추가되는 새 테스크는 `done: false`
- `src/app/owner/checklists/page.module.css`
  - `.summary`/`.summaryItem`/`.summaryNum`/`.summaryLabel` + 색상(`.sumAssigned`=primary, `.sumUnassigned`=warning, `.sumDone`=success, `.sumPending`=secondary)
  - `.empInfo`(이름+메타 컬럼)/`.empMeta`, 768 블록의 `.empName { flex:1 }` 제거
  - `.taskCheck`/`.taskCheckDone`/`.taskDone`(제목 취소선, 완료 타입 dim)

주요 결정:
- 완료/미완료 집계를 위해 `done` 상태가 필요 → 모델에 추가하고 행마다 체크박스로 토글(요약이 실시간 반영)
- 직원 할당 수는 현재 날짜 테스크 기준(`assigneeIds`에 해당 직원 포함 수)
- 요약 바는 가로 스크롤(모바일 대응), 항목별 색상으로 스캔 용이
- 빌드(`next build`) 성공 확인

## 59. 업무리스트 — 우측 하단 FAB + 테스크 만들기 팝업

사용자 요청: 우측 하단 원형 + 버튼, 누르면 메뉴(우선 "테스크 만들기"), 누르면 생성 팝업.

수정된 파일:
- `src/app/owner/checklists/page.tsx`
  - 카탈로그를 상태로 관리(`catalog`, 초기값 `TASK_CATALOG`) — 런타임 추가 가능하도록. `filteredCatalog`/`addTaskToList`가 `catalog` 참조
  - FAB 상태(`fabOpen`) + 스피드다이얼 메뉴("테스크 만들기") + 바깥 클릭 오버레이
  - 생성 팝업(`createOpen`, `newTitle`, `newType`) — 이름 input(Enter 제출) + 완료 방식 칩 선택
  - `createTask()` → `seqRef`로 `custom-N` id 생성, `catalog`에 추가(테스크 목록 패널에 즉시 노출, 드래그로 리스트 투입)
- `src/app/owner/checklists/page.module.css`
  - `.fabWrap`/`.fab`(원형, active 시 +→× 45° 회전)/`.fabMenu`/`.fabMenuItem`/`.fabOverlay`
  - `.modalOverlay`/`.modal`/`.modalHead/Body/Foot`/`.field`/`.fieldInput`/`.typeChip(+Active)`/`.btnCancel`/`.btnCreate`
  - FAB 위치: 모바일 `bottom: nav-height+16`(하단 바 위), 태블릿+ `bottom: 28px`

주요 결정:
- 생성한 테스크는 카탈로그(테스크 목록)에 추가 — 리스트에 바로 넣지 않고 드래그로 배치(기존 모델과 일관)
- FAB 메뉴는 스피드다이얼 형태로 향후 항목 확장 대비(현재 "테스크 만들기" 1개)
- 팝업은 오버레이 클릭/취소/닫기로 종료, 이름 없으면 "만들기" 비활성
- 빌드(`next build`) 성공 확인

## 60. 업무리스트 — FAB "테스크 관리"(수정/삭제) 추가

사용자 요청: FAB 메뉴에 테스크 관리 추가.

수정된 파일:
- `src/app/owner/checklists/page.tsx`
  - FAB 메뉴에 "테스크 관리"(`LiaCogSolid`) 항목 추가 (만들기 위에)
  - `manageOpen`/`editingId` 상태 + `openManage`/`startEdit`/`deleteTask`/`closeCreate`
  - 관리 팝업: 카탈로그 전체 목록(완료 방식 뱃지 + 이름 + 수정/삭제 아이콘), 하단 "새 테스크" 버튼
  - 수정은 생성 팝업을 편집 모드로 재사용(`editingId` 세팅 → 제목 "테스크 수정", 버튼 "저장")
  - `createTask`가 생성/수정 겸용: 수정 시 카탈로그 + 이미 리스트에 배치된 인스턴스(`catalogId` 일치)도 함께 갱신(제목/완료방식 동기화)
  - 삭제는 카탈로그에서만 제거(이미 배치된 인스턴스는 자체 데이터 보유로 유지)
- `src/app/owner/checklists/page.module.css`
  - `.manageBody`(스크롤)/`.manageRow`/`.manageType`/`.manageName`/`.manageEdit`/`.manageDelete`
  - `.modal { max-height: 85vh }`, `.btnCreate` inline-flex(아이콘+텍스트 정렬)

주요 결정:
- 수정 UI는 생성 팝업 재사용(중복 최소화) — 편집 모드 분기(`editingId`)
- 수정 시 배치된 인스턴스까지 동기화(제목/타입) → 관리에서 바꾼 내용이 리스트에도 반영
- 삭제는 카탈로그만 — 이미 배치된 업무는 보존(작업 손실 방지)
- 빌드(`next build`) 성공 확인

## 61. 업무리스트 — 업무 클릭 드롭다운 + 수행 방법 팝업

사용자 요청: 업무를 누르면 드롭다운 메뉴(우선 "수행 방법"), 누르면 팝업으로 수행 방법 표시. 수행 방법은 테스크 등록 시 함께 기재.

수정된 파일:
- `src/mock/tasks.ts` — `TaskTemplate`/`StoreTask`에 `method: string`(수행 방법) 추가, 카탈로그 12개에 수행 방법 문구 기재, `makeTask`에서 복사
- `src/app/owner/checklists/page.tsx`
  - 업무 제목을 버튼으로 변경(`taskMain` + `taskTitleBtn` + chevron) — 클릭 시 드롭다운(`menuTaskId`)
  - 드롭다운 메뉴 항목 "수행 방법"(`LiaBookSolid`) → `methodTask` 설정으로 수행 방법 팝업 오픈, 바깥 클릭 오버레이(`menuOverlay`)
  - 수행 방법 팝업: 완료방식 뱃지 + 제목 + 방법 텍스트(없으면 "등록된 수행 방법이 없습니다")
  - 생성/수정 팝업에 "수행 방법" textarea(`newMethod`) 추가, `createTask`/`startEdit`/`addTaskToList`에 method 반영, 수정 시 배치 인스턴스도 method 동기화
- `src/app/owner/checklists/page.module.css`
  - `.taskMain`/`.taskTitleBtn`/`.taskTitleChevron`/`.taskMenu`/`.taskMenuItem`/`.menuOverlay`
  - `.fieldTextarea`, `.methodBody`/`.methodTaskName`/`.methodText`/`.methodEmpty`
  - `.modalBody { overflow-y: auto }`

주요 결정:
- 드롭다운은 여러 항목 확장 대비(현재 "수행 방법" 1개) — 추후 항목 추가 용이
- 수행 방법은 `StoreTask`에도 복사 + 수정 시 인스턴스 동기화(팝업이 항상 최신 반영)
- 메뉴 z-index: 오버레이 45 / 메뉴 46 (모달 300보다 아래)
- 빌드(`next build`) 성공 확인

## 62. 수행 방법 — 게시글 형태 에디터(텍스트+인라인 이미지)

사용자 요청: 사진 첨부 형태 말고, 게시글 작성처럼 에디터로. (앞서 넣은 별도 사진 첨부 방식 대체)

수정된 파일:
- `src/mock/tasks.ts` — `methodPhotos` 제거, `method`가 게시글 HTML(텍스트+인라인 이미지) 저장
- `src/app/owner/checklists/page.tsx`
  - 생성/수정 팝업의 "수행 방법"을 `contentEditable` 에디터로 교체(툴바 "사진" 버튼 + 인라인 이미지 삽입)
  - `editorRef` + `useEffect([createOpen])`로 열릴 때 초기 HTML 주입(uncontrolled), 저장 시 `editorRef.innerHTML` 읽기
  - `insertPhotos`: 파일 선택 → `URL.createObjectURL` → `document.execCommand('insertHTML', <img>)`로 커서 위치 삽입
  - 수행 방법 팝업: `hasMethodContent()` 판정 후 `dangerouslySetInnerHTML`로 게시글 렌더
  - `newPhotos`/`removePhoto`/textarea 제거
- `src/app/owner/checklists/page.module.css`
  - `.photoRow`/`.photoThumb`/`.photoAdd`/`.fieldTextarea`/`.methodPhotos` 제거
  - `.editorToolbar`/`.editorTool`/`.editor`(빈 상태 placeholder, 이미지 스타일) + `.methodContent`(게시글 뷰, 이미지)

주요 결정 / 한계:
- 프로토타입 경량 에디터: `contentEditable` + `execCommand('insertHTML')` (에디터 라이브러리 미도입) — 추후 정식 에디터로 교체 가능
- 이미지는 `blob:` object URL (세션 한정, 새로고침 시 소실) — 목업 범위
- 팝업은 `dangerouslySetInnerHTML` 사용: 콘텐츠가 사장 본인 작성(프로토타입)이라 허용 — 실서비스는 sanitize 필요
- 빌드(`next build`) 성공 확인

## 63. 수행 방법 에디터 — Lexical로 교체

사용자 요청: Lexical 에디터 사용.

패키지 추가: `lexical`, `@lexical/react`, `@lexical/html` (0.46.0)

추가된 파일:
- `src/app/owner/checklists/MethodEditor.tsx` (`'use client'`)
  - `LexicalComposer` + `RichTextPlugin`(ContentEditable, placeholder) + `HistoryPlugin`
  - 커스텀 `ImageNode`(DecoratorNode<JSX.Element>) — 인라인/블록 이미지, `importDOM`/`exportDOM`으로 `<img>` HTML 라운드트립, `decorate()`로 React 렌더
  - `InitContentPlugin`: 최초 마운트 시 initialHtml을 `$generateNodesFromDOM` + `$insertNodes`로 주입(uncontrolled)
  - `OnChangeHtmlPlugin`: 변경 시 `$generateHtmlFromNodes`로 HTML 직렬화 → onChange
  - `EditorToolbar`: "사진" 버튼 → 파일 선택 → `URL.createObjectURL` → `$createImageNode` 삽입(선택 없으면 root append)

수정된 파일:
- `src/app/owner/checklists/page.tsx`
  - 기존 `contentEditable`/`execCommand`/`editorRef`/`insertPhotos`/init `useEffect` 제거
  - `<MethodEditor key={editingId ?? 'new'} initialHtml={methodHtmlRef.current} onChange={html => methodHtmlRef.current = html} />`
  - `newMethod` state → `methodHtmlRef`(ref)로 변경(키 입력마다 페이지 리렌더 방지), 저장 시 `methodHtmlRef.current` 사용
  - `LiaImageSolid`·`useEffect` import 제거(에디터로 이동)
- `src/app/owner/checklists/page.module.css` — `.editorShell`(relative) + `.editorPlaceholder`(absolute) 추가, `.editor:empty::before` 제거(Lexical placeholder 사용)

주요 결정 / 한계:
- 저장 포맷은 HTML(`@lexical/html`) — 팝업은 기존 `dangerouslySetInnerHTML` 그대로 재사용, `ImageNode`가 `<img>`로 export/import
- 에디터는 생성/수정 모달(`createOpen`) 안에서만 렌더 → SSR에서 Lexical 미실행(정적 프리렌더 유지)
- 이미지 `blob:` URL은 여전히 세션 한정(실서비스는 업로드 연동 필요), 팝업 HTML은 sanitize 필요(프로토타입이라 미적용)
- 빌드(`next build`) 성공 확인

## 64. 에디터 공용 컴포넌트화 + 초기화 방식 견고화

사용자 요청: 테스크 만들기/수정 시 에디터가 제대로 적용되게, 그리고 에디터는 여러 곳에서 재사용 가능하게.

원인/조치:
- 이전 `MethodEditor`의 초기 내용 주입이 `InitContentPlugin`에서 `root.select()` + `$insertNodes`로 처리 → 빈 root selection 등 런타임 취약점. 편집 중 Fast Refresh 깨짐과 겹쳐 "에디터 미적용"으로 보였음.
- **초기화를 Lexical `initialConfig.editorState` 함수(`prepopulate`)로 이동** — 에디터 생성 시점에 실행, selection 조작 없이 노드 append(인라인/텍스트는 문단 래핑). 훨씬 안전.

추가된 파일:
- `src/components/RichTextEditor/RichTextEditor.tsx` — 공용 리치 텍스트 에디터(`initialHtml`, `onChange`, `placeholder` props). `ImageNode`/`Toolbar`/`OnChangeHtmlPlugin` 포함
- `src/components/RichTextEditor/RichTextEditor.module.css` — 자체 스타일(툴바/에디터/placeholder/이미지) — 특정 페이지 CSS 비의존

삭제/정리:
- `src/app/owner/checklists/MethodEditor.tsx` 삭제
- `page.module.css`에서 `.editor*`/`.editorToolbar`/`.editorShell`/`.editorPlaceholder` 제거(공용 컴포넌트로 이동)

수정된 파일:
- `src/app/owner/checklists/page.tsx` — `@/components/RichTextEditor/RichTextEditor` import, `<RichTextEditor initialHtml placeholder onChange />` 사용

주요 결정:
- 에디터는 `src/components`로 승격해 어디서든 재사용(요청 반영), 스타일도 자체 모듈로 독립
- 초기 HTML 주입은 `editorState` 초기화 함수 방식이 표준·안전 — 플러그인/effect 방식 폐기
- 빌드(`next build`) 성공, 개발 서버 라우트 재컴파일 정상(200) 확인

## 65. 에디터 미표시 재현/검증 — 코드 정상, 브라우저 캐시 이슈로 결론

사용자 보고: `npm i` + 재시작해도 에디터가 적용 안 됨.

조치(진단):
- 헤드리스 브라우저(puppeteer)로 실제 모달을 열어 검증:
  - **테스크 만들기**: contenteditable 384×150px, `data-lexical-editor="true"`, 오류 0
  - **테스크 수정**: 기존 수행 방법 텍스트가 에디터에 정상 로드, 오류 0
- 서비스워커/next-pwa 없음 확인 → SW 캐시 원인 아님
- 결론: 코드는 정상. 사용자 브라우저의 stale 번들(하드 리프레시/포트 혼동 추정)

정리:
- 진단용 `puppeteer` devDependency 제거, 임시 스크립트 삭제 (lexical 의존성은 유지)

권장 조치(사용자):
- 하드 리프레시(Ctrl+Shift+R), 개발 서버 실제 포트 확인(3000 점유 시 3001 등으로 뜸), 시크릿 창/사이트 데이터 삭제

## 66. 테스크 만들기 — 웹 팝업 확대 + 주기(반복/단건) 설정

사용자 요청: 웹에서 생성 팝업 크게. 테스크가 주기적(반복)인지 단건인지 설정. (구조: Task → 업무 리스트, 현재 모델과 일치)

수정된 파일:
- `src/mock/tasks.ts` — `Recurrence`('RECURRING'|'ONCE') 타입, `TaskTemplate.recurrence?`/`StoreTask.recurrence` 추가, `makeTask` 기본 'RECURRING', t7(교육자료) 데모로 'ONCE'
- `src/app/owner/checklists/page.tsx`
  - `newRecurrence` 상태 + `RECURRENCE_OPTIONS`/`RECURRENCE_LABEL`
  - 생성/수정 팝업에 "주기" 필드(반복/단건 칩) + 힌트 문구
  - `createTask`(생성/수정/인스턴스 동기화)·`startEdit`·`openCreateTask`·`addTaskToList`에 recurrence 반영
  - 테스크 관리 목록 행에 주기 배지(반복=파랑, 단건=회색)
- `src/app/owner/checklists/page.module.css`
  - `.modal` max-width 420 → 560(768+) → 640(1024+)
  - `.fieldHint`, `.recurBadge`/`.recurRecurring`/`.recurOnce`

주요 결정:
- 주기는 Task(카탈로그 템플릿)의 속성 — 인스턴스에 복사 + 수정 시 동기화(method와 동일 패턴)
- 우선 반복/단건 이분 설정만(주기 상세: 매일/매주 등은 추후 확장 여지)
- DB 구조(Task → 업무 리스트)는 현재 카탈로그(Task)→공통/추가 리스트 구조와 일치, 별도 변경 불필요
- 빌드(`next build`) 성공 확인

## 67. 반복 주기 상세 설정 (요일/월초·월말/N개월·N년/몇째주 요일)

사용자 요청: 반복일 때 언제 반복하는지 상세 설정(요일별, 월초/월말, N개월·N년 주기, 몇째주 무슨 요일 등).

수정된 파일:
- `src/mock/tasks.ts`
  - `RecurrenceFreq`('DAILY'|'WEEKLY'|'MONTHLY'|'YEARLY'), `MonthlyMode`('DAY'|'NTH_WEEKDAY'), `MonthDayKind`('FIRST'|'LAST'|'SPECIFIC'), `RecurrenceRule` 인터페이스 추가
  - `DEFAULT_RECURRENCE_RULE`(매일), `TaskTemplate.recurrenceRule?`/`StoreTask.recurrenceRule`, `makeTask` 기본값
  - 데모: t6=매월 월말, t9=매주 월·목
- `src/app/owner/checklists/page.tsx`
  - `RecurrenceEditor` 컴포넌트: 빈도(매일/매주/매월/매년) + 간격(N마다) + WEEKLY 요일 선택 + MONTHLY(날짜지정: 월초/월말/특정일 / 요일지정: 몇째주+요일) + 요약 문구
  - `describeRecurrence(rule)` — 사람이 읽는 요약("매주 월·목", "매월 월말", "3개월마다", "매월 둘째 화요일" 등)
  - `newRule` 상태 + create/edit/instance 동기화, `addTaskToList` 반영
  - 반복 선택 시 팝업에 RecurrenceEditor 표시, 단건이면 안내 문구
  - 테스크 관리 배지: 반복이면 `describeRecurrence`, 단건이면 "단건"
- `src/app/owner/checklists/page.module.css` — `.recurDetail`/`.recurRow`/`.recurNum`/`.recurUnit`/`.recurWeekdays`/`.recurDay(+On)`/`.recurSummary`

주요 결정:
- `RecurrenceRule` 하나로 요청된 모든 케이스 커버(요일별·월초/월말·N개월·N년·몇째주 요일)
- 요일 인덱스 0=월..6=일(주기 설정 자체 기준, 프로토타입에선 실제 스케줄링과 분리)
- 요약(`describeRecurrence`)을 폼·관리 배지에 공통 사용
- 빌드(`next build`) 성공 확인

## 68. 에디터 서식 툴바 + 생성 팝업 재디자인(뱃지 제거·2단·고정 크기)

사용자 요청: (1) 에디터가 "이거(Lexical 문서의 툴바 있는 에디터)"인데 왜 적용 안 됨 → 서식 툴바 필요, (2) 주기 설정이 너무 뱃지 스타일 → 정리, (3) 웹에서 가로로 넓게 스크롤 없이, (4) 팝업 더 크게·크기 고정·반복/단건으로 길어지면 그 부분만 스크롤.

패키지 추가: `@lexical/list`, `@lexical/rich-text`, `@lexical/selection`, `@lexical/utils`

수정된 파일:
- `src/components/RichTextEditor/RichTextEditor.tsx`
  - 서식 툴바(`Toolbar`): 굵게/기울임/밑줄(`FORMAT_TEXT_COMMAND`), 제목 H2(`$setBlocksType`+`$createHeadingNode`), 글머리/번호 목록(`@lexical/list` 명령), 사진. 선택 상태 하이라이트(`registerUpdateListener`)
  - 노드 등록: ImageNode + HeadingNode/QuoteNode/ListNode/ListItemNode, `<ListPlugin />` 추가
- `src/components/RichTextEditor/RichTextEditor.module.css` — 툴바 버튼/구분선/활성 상태, 에디터 콘텐츠 서식(h2/ul/ol/li/img)
- `src/app/owner/checklists/page.tsx`
  - 완료 방식: 칩 → `<select>`, 반복/단건: 칩 → 세그먼트 컨트롤
  - `RecurrenceEditor`: 빈도/월별모드/월초·월말·특정일/몇째주 → `<select>`로 교체(요일 선택은 원형 유지) — 뱃지 최소화
  - 모달 본문을 2단(설정 col | 에디터 col)으로
- `src/app/owner/checklists/page.module.css`
  - `.select`/`.segmented`/`.segBtn(+Active)`/`.formCol` 추가, orphan `.typeChip*` 제거
  - 모달: 웹에서 `max-width` 860→980px, **고정 높이**(`height: 88vh; max-height: 720px`), 본문 2단(`row`), 컬럼별 내부 스크롤(`.formCol { overflow-y: auto }`), 헤더/푸터 `flex-shrink: 0`
  - 팝업 뷰 `.methodContent`에 h2/ul/ol/li 서식 추가

검증(headless puppeteer): 생성 모달에서 툴바(굵게/제목/사진)·2단·셀렉트·세그먼트·요약 모두 렌더, 오류 0 확인. (진단용 puppeteer는 검증 후 제거)

주요 결정:
- "에디터 미적용"의 실체는 **서식 툴바 부재**로 리치 에디터처럼 안 보였던 것 → 툴바 추가로 해결(렌더 자체는 이전에도 정상이었음)
- 팝업 크기 고정 + 컬럼 내부 스크롤 → 반복 상세로 길어져도 모달이 안 커지고 왼쪽 설정 컬럼만 스크롤
- 빌드(`next build`) 성공 확인

## 69. 에디터 — 툴바+작성영역 단일 프레임 통합 + 작성영역 세로 꽉차게

사용자 요청: 수행 방법에서 에디터(툴바)와 작성 부분을 합치고, 작성 부분을 세로 길이 꽉차게.

수정된 파일:
- `src/components/RichTextEditor/RichTextEditor.tsx` — 툴바 + 작성영역을 하나의 `.frame`으로 감쌈
- `src/components/RichTextEditor/RichTextEditor.module.css`
  - `.frame`(단일 테두리/라운드, `flex:1`, `min-height:240px`, `focus-within` 강조) — 툴바는 하단 테두리만, 에디터는 자체 테두리 제거
  - `.shell { flex:1; display:flex; flex-direction:column }` + `.editor { flex:1; min-height:0; overflow-y:auto }` → 작성 영역이 프레임 높이를 채움
- `src/app/owner/checklists/page.tsx` — 수행 방법 필드에 `fieldEditor` 클래스
- `src/app/owner/checklists/page.module.css` — `.fieldEditor { flex:1; min-height:0 }`, `.formCol` flex:1을 웹(768+)로 이동(모바일은 자연 스택)

검증(headless): 모달 720×980 고정, 오른쪽 컬럼 558px 중 프레임 531px·에디터 488px(툴바 41px 제외 전부 채움), 툴바가 프레임 내부(단일 박스), 오류 0.

주요 결정:
- flex 체인(formCol→fieldEditor→frame→shell→editor 모두 flex:1/min-height:0)으로 고정 높이 모달에서 작성 영역이 남는 세로 공간을 채움
- 툴바/에디터 개별 테두리 제거 → 프레임 하나로 통합된 에디터 박스
- 빌드(`next build`) 성공 확인

## 70. 테스크 모델 정리 — 완료 방식 제거 + 반복 업무 담당자

사용자 요청: 완료 방식 없앰. 테스크는 이름·수행방법·주기. 추가로 반복 업무일 때 담당자 지정.

수정된 파일:
- `src/mock/tasks.ts`
  - `TaskTemplate`/`StoreTask`에서 `completionType` 제거, `CompletionType` import 제거
  - `TaskTemplate.defaultAssigneeIds?` 추가(반복 업무 기본 담당자), 카탈로그 t1/t2/t6에 기본 담당자 지정
  - `DEFAULT_ASSIGN` 맵 제거 → `makeTask`가 `tpl.defaultAssigneeIds`로 인스턴스 `assigneeIds` 시드
- `src/app/owner/checklists/page.tsx`
  - `COMPLETION_LABEL`/`COMPLETION_OPTIONS`/`newType` 제거, 생성 모달의 완료 방식 select 제거
  - 완료 방식 뱃지 제거: 업무 행/카탈로그 항목/관리 목록/수행방법 팝업
  - `newAssignees` 상태 + 반복 선택 시 "담당자" 멀티 선택 피커(직원 아바타+이름 토글)
  - `createTask`: `defaultAssigneeIds` 저장(반복일 때만), 수정 시 인스턴스는 제목/방법/주기만 동기화(assigneeIds는 미변경 — 개별 배정 보존), `addTaskToList`는 `defaultAssigneeIds`로 시드
- `src/app/owner/checklists/page.module.css`
  - orphan 제거: `.taskCompletion`(+`.taskDone .taskCompletion`)/`.manageType`/`.catalogCompletion`
  - `.assigneePick`/`.assigneePickItem`/`.assigneePickOn`/`.assigneePickAvatar`(담당자 토글) 추가

주요 결정:
- 테스크 필드 = 이름 + 수행방법 + 주기(+반복 시 담당자). 완료 방식은 전 UI에서 제거
- 담당자는 Task(템플릿)의 `defaultAssigneeIds` — 리스트에 편성될 때 인스턴스 배정으로 시드, 수정해도 기존 인스턴스의 개별 배정은 유지
- `mockChecklists`(직원 수행 페이지)의 `completionType`은 별개 타입이라 미변경
- 빌드(`next build`) 성공 확인

## 71. 담당자 — 칩 토글 → 리스트 + "담당자 추가하기" 팝업

사용자 요청: 담당자를 칩 토글 말고 리스트 형태로, 옆에 "담당자 추가하기" 버튼 → 누르면 직원 목록 팝업.

수정된 파일:
- `src/app/owner/checklists/page.tsx`
  - `dutyPickerOpen` 상태 + `toggleAssignee` 함수, `closeCreate`에서 팝업 닫기
  - 담당자 필드: 헤더(라벨 + "담당자 추가하기" 버튼) + 선택된 담당자 리스트(아바타·이름·× 제거), 없으면 "지정된 담당자가 없습니다"
  - 직원 선택 팝업(`dutyPop*`): 오버레이 + 카드 + 활성 직원 목록, 선택 시 체크(`LiaCheckSolid`) 토글
- `src/app/owner/checklists/page.module.css`
  - 기존 `.assigneePick*` 제거
  - `.dutyHead`/`.dutyAdd`/`.dutyList`/`.dutyRow`/`.dutyAvatar`/`.dutyName`/`.dutyRemove`/`.dutyEmpty` + 팝업 `.dutyPop*`(z-index 320, 생성 모달 300 위)

주요 결정:
- 팝업은 formCol `overflow` 클리핑을 피해 fixed 오버레이 모달로(생성 모달 위 z-index 320)
- 팝업에서 다중 선택(체크 토글), 리스트에서 개별 ×로 제거 — 양쪽 동일 `toggleAssignee`
- 빌드(`next build`) 성공 확인

## 72. 반복 섹션 등장/퇴장 애니메이션

사용자 요청: 반복 선택 시 창 나올 때 애니메이션, 단건으로 바꿀 때 열려있던 창 사라지는 것도 애니메이션.

수정된 파일:
- `src/app/owner/checklists/page.tsx`
  - 반복 상세(RecurrenceEditor) + 담당자 필드를 하나의 `recurSection` 래퍼로 묶음
  - `recurExiting` 상태 + `setRecurrence(value)` — 단건 전환 시 즉시 언마운트하지 않고 `recurExiting=true`로 210ms 유지 후 언마운트(퇴장 애니메이션 재생), 반복 전환은 즉시 등장
  - 세그먼트 onClick을 `setRecurrence`로, 렌더 조건 `(RECURRING || recurExiting)`, 클래스 `recurExiting ? recurExit : recurEnter`
  - `openCreateTask`/`startEdit`에서 `recurExiting` 초기화
- `src/app/owner/checklists/page.module.css`
  - `@keyframes revealDown`(등장) + `collapseUp`(퇴장), `.recurSection`/`.recurEnter`/`.recurExit`(`pointer-events:none`)
  - `.recurDetail`의 개별 애니메이션 제거(래퍼가 담당), 이전 `.reveal` 제거

주요 결정:
- 퇴장 애니메이션은 CSS만으로 불가(언마운트 즉시) → `recurExiting` 플래그로 언마운트 지연(setTimeout 210ms ≈ 애니메이션 0.2s)
- 반복 상세 + 담당자를 한 섹션으로 묶어 함께 슬라이드 인/아웃
- 빌드(`next build`) 성공 확인

## 73. 테스크 설정 3종 추가 — 카테고리(관리형) · 수행 시점(시간 범위) · 반복 기간

사용자 요청(선택): 수행 시점(시간대·마감시각), 반복 시작·종료일, 카테고리/태그. 이어서 "카테고리는 추가/삭제/이름수정 가능하게(드롭다운 옆 ≡ 버튼)", "수행 시점은 시작~종료 시간 범위로".

수정된 파일:
- `src/mock/tasks.ts`
  - `TaskTiming`을 시간 범위로: `{ start: 'HH:MM'|''; end: 'HH:MM'|'' }`(둘 다 비면 상시), `DEFAULT_TIMING`
  - 카테고리 관리형: `TaskCategory = string`(id), `Category { id, name }`, `DEFAULT_CATEGORIES`(준비/청소/위생/재고/안전/마감/기타)
  - `TaskTemplate`/`StoreTask`에 `category`·`timing` 추가, `TaskTemplate`에 `recurStart?`·`recurEnd?`
  - `makeTask` 기본값, 카탈로그 데모(t1 08:30~09:00/준비, t5 22:00~22:30/마감, t9 ~15:00/위생)
- `src/app/owner/checklists/page.tsx`
  - 상태: `newCategory`/`newTiming`/`newRecurStart`/`newRecurEnd` + 카테고리 관리(`categories`/`catManageOpen`/`catDraft`/`catSeqRef`)
  - `categoryName(id)`, `addCategory`/`renameCategory`/`deleteCategory`(현재 선택 삭제 시 첫 항목으로 대체), `describeTiming`(범위→"09:00~11:00"/"~15:00"/"상시"), `hasTiming`
  - 폼: 카테고리(select + ≡ 관리 버튼), 수행 시점(시작~종료 time input 2개), 반복 기간(시작~종료 date input, recurSection 내부)
  - 카테고리 관리 팝업: 목록별 이름 인라인 수정 + 삭제, 하단 새 카테고리 추가
  - 표시: 업무 행 수행시점 배지(상시 아니면), 관리 목록/수행방법 팝업에 카테고리 배지(`categoryName`) + 메타(시점·주기)
  - create/edit/instance 동기화·`addTaskToList`에 category/timing 반영(recurStart/End는 템플릿 전용)
- `src/app/owner/checklists/page.module.css` — `.catRow`/`.catManageBtn`, 카테고리 관리 팝업(`.catManage*`/`.catAdd*`), `.timeInput`/`.dateInput`, `.taskTiming`/`.catBadge`/`.methodMeta`

주요 결정:
- 카테고리는 고정 enum → 사용자 관리 목록(state). id 참조, 삭제된 카테고리는 `미분류` 폴백
- 수행 시점은 시작~종료 범위(한쪽만/양쪽 비움 허용) — 프리셋 방식 폐기
- 반복 기간(시작·종료일)은 반복일 때만, 종료 비우면 무기한
- '필수/선택'은 사용자가 미선택 → 추가 안 함
- 빌드(`next build`) 성공 확인

## 74. 수행 시점 — 가로 드래그 타임라인 슬라이더

사용자 요청: 시간 입력도 좋지만 가로 그래프에서 드래그로 시간대를 설정하고 싶음.

수정된 파일:
- `src/app/owner/checklists/page.tsx`
  - `hmToMin`/`minToHm` 헬퍼 + `TimeRangeSlider` 컴포넌트(06:00~24:00 트랙, 30분 스냅)
    - 시작/종료 핸들 드래그로 각 시각 조정, 가운데 채움(fill) 드래그로 범위 이동, pointer 이벤트(window listener)로 처리
    - 비활성(상시)일 때 기본 09~18 흐리게 + "드래그해서 시간대 설정" 힌트, 드래그 시 활성화
  - 수행 시점 필드: 슬라이더 + 기존 시간 입력(동기화) + "상시" 초기화 버튼
- `src/app/owner/checklists/page.module.css` — `.trs*`(트랙/채움/핸들/힌트/눈금/값), `.trsClear`

주요 결정:
- 슬라이더 ↔ time input 양방향 동기화(같은 `newTiming`), "상시" 버튼으로 비움
- 06:00~24:00 범위·30분 단위(가게 운영시간 기준) — 그 밖 시각은 time input으로 직접 입력
- 빌드(`next build`) 성공 확인

## 75. 수행 시점 슬라이더 — 상단 시간 편집형 + 하단 입력창 제거

사용자 요청: 슬라이더 위 시간 부분에서 상세 수정, 아래 시간 입력창 제거.

수정된 파일:
- `src/app/owner/checklists/page.tsx`
  - `TimeRangeSlider` 상단(`trsValue` 텍스트) → 편집형 `trsTop`(시작/종료 `type=time` 입력 2개 + "~" + 활성 시 "상시로" 버튼)
  - 수행 시점 필드 하단의 별도 time input 행 제거(슬라이더 컴포넌트 하나로 통합)
- `src/app/owner/checklists/page.module.css` — `.trsValue` → `.trsTop`/`.trsTimeInput`/`.trsSep`

주요 결정:
- 정밀 수정은 슬라이더 상단 시간 입력, 대략 설정은 핸들 드래그 — 한 컴포넌트로 통합
- 빌드(`next build`) 성공 확인

## 76. 테스크 관리 — 리스트 | 상세 2-pane 재설계 (+활성/비활성·검색)

사용자 요청(연속): 관리 창을 왼쪽 리스트 + 오른쪽 상세 설정으로. 가로 더 넓게. 활성/비활성. 목록 이름·카테고리 검색. 새 테스크 버튼은 리스트 맨 아래.

수정된 파일:
- `src/mock/tasks.ts` — `TaskTemplate.active?`(undefined=활성), 데모 t11 비활성
- `src/app/owner/checklists/page.tsx`
  - 생성 모달(`createOpen`) 제거 → 관리 창으로 통합. 폼 본문을 `renderTaskFormBody()`로 추출해 상세 pane에서 재사용
  - 함수 재편: `createTask→saveTask`(생성 시 새 id 선택·모달 유지), `openCreateTask→openManageNew`, `startEdit→selectTask`(상태만 로드), `selectNew`, `closeCreate→closeManage`, `openManage`(첫 항목 선택), `deleteTask`(삭제 후 인접 선택)
  - FAB "테스크 만들기"/"테스크 관리" 모두 관리 창을 열되 초기 선택만 다름
  - 관리 창: 좌측 리스트(카테고리 배지+이름, 선택 강조, 비활성 흐림+"비활성" 배지) + 하단 "새 테스크" 버튼, 상단 검색(이름·카테고리) / 우측 상세 폼(+상태 활성/비활성 세그먼트) + 저장·삭제
  - 카탈로그 드래그 패널은 비활성 제외(`activeCatalog`), 관리 목록은 `manageFiltered`(이름 or 카테고리명)
- `src/app/owner/checklists/page.module.css`
  - `.manageModal`(웹 `94vw`/최대 1400px·고정 높이 800px), `.manageLayout`(행), `.manageList`(260px)/`.manageListScroll`/`.manageListItem(+Active/+Off)`/`.manageListName`/`.offBadge`, `.manageDetail`, `.manageNewBtn`(하단), `.manageSearch*`/`.manageEmpty`, `.btnDanger`
  - orphan 제거: 구 관리 리스트(`.manageBody`/`.manageRow`/`.manageName`/`.manageEdit`/`.manageDelete`/`.recurBadge`/`.recurRecurring`/`.recurOnce`)

주요 결정:
- 생성/수정/관리를 하나의 2-pane 창으로 통합(폼 중복 제거) — `renderTaskFormBody()` 공유, `saveTask`가 생성 후 선택 유지
- 비활성 테스크는 드래그 카탈로그·데일리 편성에서 제외, 관리 창에서만 흐리게 노출(재활성 가능)
- 헤드리스 검증: 모달 1400×800, 리스트 260/상세 1140, 12항목·첫 선택·에디터 568px, "새 테스크"→빈 폼, 오류 0
- 빌드(`next build`) 성공 확인

## 77. 전역 삭제 확인(컨펌) 시스템

사용자 요청: 시스템의 모든 삭제 버튼은 누르면 컨펌 창. good에 규칙 추가. 이어서 컨펌 창 버튼은 컨펌 왼쪽·취소 오른쪽.

추가된 파일:
- `src/context/ConfirmContext.tsx` — `ConfirmProvider` + `useConfirm()` (옵션: title/message/confirmText/cancelText/danger). `confirm()`이 Promise<boolean> 반환, 확인 모달 렌더(z-index 500)
- `src/context/ConfirmContext.module.css` — 다이얼로그 스타일(컨펌=빨강 danger 기본)

수정된 파일:
- `src/app/layout.tsx` — `ConfirmProvider`를 ToastProvider 안쪽에 주입
- `src/app/owner/checklists/page.tsx` — `useConfirm`으로 `deleteTask`/`deleteCategory`/`removeTask`(× 업무 제거)를 async 확인 후 삭제로
- `src/app/owner/requests/[id]/page.tsx` — 기존 커스텀 삭제 확인 팝업 제거 → 공용 `useConfirm`으로 통일(`deleteRequest` async)
- `.claude/good-bad.md` — Good에 "모든 삭제 버튼 컨펌" + "컨펌 왼쪽·취소 오른쪽" 규칙 추가

주요 결정:
- 모든 삭제를 공용 `useConfirm()`으로 단일화 — 앞으로 삭제/파괴적 액션은 이걸 사용
- 컨펌 창 버튼 순서: 컨펌(삭제) 왼쪽 / 취소 오른쪽 (ConfirmContext 한 곳에서 전역 반영)
- 담당자 칩 × 등 미세 제거(edit성)는 컨펌 대상에서 제외
- 헤드리스 검증: 삭제 클릭→확인창(취소/삭제), 취소=유지, 확인=삭제, 오류 0 / 빌드 성공

## 78. 이벤트 완료 토스트

사용자 요청: 삭제·저장 등 이벤트 완료 시 토스트 메세지. good에 규칙 추가.

수정된 파일:
- `.claude/good-bad.md` — Good에 "삭제·저장·추가 완료 시 `useToast()`로 토스트" 규칙 추가
- `src/app/owner/checklists/page.tsx` — `useToast` 연결, `saveTask`(저장/추가)·`deleteTask`·`deleteCategory`·`removeTask`·`addCategory` 완료 시 토스트

주요 결정:
- 토스트 z-index 9999 > 모달/컨펌(300/500)이라 모달 위에서도 정상 노출
- 드래그로 리스트에 추가는 빈번한 유동 동작이라 토스트 제외(시각 피드백으로 충분)
- 요청 상세는 기존에 토스트 사용 중(변경 없음)
- 빌드(`next build`) 성공 확인
