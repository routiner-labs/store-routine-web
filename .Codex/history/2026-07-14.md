## Claude request rollback updates

- Files changed:
  - `C:\Users\chey.kim\Documents\GitHub\limo\.claude\requests\2026-07-13\lm-label-add-tran-item-id.sql`
  - `C:\Users\chey.kim\Documents\GitHub\limo\.claude\requests\2026-07-14\inbound-inflight-inspection-cleanup.sql`
  - `C:\Users\chey.kim\Documents\GitHub\limo\.claude\requests\2026-07-13\lm-label-add-tran-item-id-rollback.sql`
  - `C:\Users\chey.kim\Documents\GitHub\limo\.claude\requests\2026-07-14\inbound-inflight-inspection-cleanup-rollback.sql`
- Reason:
  - Moved backup SQL from rollback files into the original 2026-07-13 and 2026-07-14 data-changing SQL files so backup is created before any destructive change runs.
- Key decisions:
  - The 2026-07-13 rollback restores the old schema only after backfilling `lm_label.inven_id` and aborts if any NULL remains.
  - The 2026-07-14 rollback is backup-driven because the forward script deletes inventory/relationship rows and cannot be safely reconstructed from live state alone.
  - Backup SQL was removed from both rollback files.
  - The 2026-07-14 forward SQL now creates backup tables after fixing the migration target in temporary tables and before the first data change.
  - Each 2026-07-14 backup uses the same predicate as its corresponding `UPDATE` or `DELETE`, so only affected rows are stored.
  - The 2026-07-13 migration keeps a full `lm_label` backup because its DDL applies to the entire table and has no row-level target predicate.
  - Rollback restores original `upd_time` values and identifies `lm_tran_inven_rel` rows by its primary key.
  - Existing backup table names intentionally cause the forward SQL to fail instead of silently overwriting a previous backup.
- Screens/features:
  - None. SQL request artifacts only.
- Verification:
  - Confirmed the order is target snapshot, safety checks, scoped backups, then data changes.
  - Ran `EXPLAIN` for all five scoped backup queries against the local PostgreSQL schema inside a transaction and forced rollback; all passed.
