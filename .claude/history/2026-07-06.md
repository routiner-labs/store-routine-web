# 2026-07-06 작업 기록

## 목업 전체 분석 - 부족한 기능 일괄 추가 (직원 측 대규모 보강)

### 분석 결과 (부족했던 것)
직원(알바) 측 공백: 내 요청 목록/상세 없음, 문서함 없음, 스케줄 확인 없음, 출근하기 dead button, 특별지시 열람 불가, `requests/new`의 `?type=` 쿼리 미사용. 사장 측: 스케줄 입력 기능 없음, 가입신청 처리 피드백 없음.

### 신규 페이지 (직원)
- `/employee/requests` — 내 요청함: 상태 칩 필터(건수), 검색(사장 헤더 패턴), 새 요청 FAB.
- `/employee/requests/[id]` — 요청 상세: 상태 안내 배너, 본문/사진, 댓글 목록+작성(사장 뱃지).
- `/employee/documents` — 문서함: 검색 + 카테고리 칩 + 목록, 새 문서 FAB.
- `/employee/documents/new` — 문서 작성 (`DocumentForm` 재사용).
- `/employee/documents/[id]` — 문서 열람. **본인 작성 문서(authorName=이지은)만 수정/삭제 가능** (내 문서 뱃지).
- `/employee/schedule` — 내 스케줄: 요약 4종(주 근무일/시간, 출/퇴근 시각), 이번 주 7일 카드(오늘 강조), 근무 패턴 카드.

### 직원 구성/디자인을 사장과 통일
- `EmployeeNav` 재작성: OwnerNav와 동일 구조(사이드바+그룹[업무: 요청함·문서함 / 스케줄: 내 스케줄]+유저 섹션, 모바일 3분할 바[알림/메뉴/계정]+드로어+알림 전체화면). `OwnerNav.module.css` 직접 재사용으로 디자인 동일. 직원관리 등 사장 전용 메뉴는 제외.
- `employee/layout` — 사장 레이아웃과 동일한 그리드(사이드바+콘텐츠) + 모바일 상단 매장명 바(StoreBar 스타일 재사용).
- 직원 요청함/문서함 헤더를 사장 목록 페이지 패턴(heading+인라인 검색+검색 버튼, 모바일은 검색 숨김)으로 통일. 작성 버튼은 good-bad 규칙대로 FAB.
- 웹=밀도: 직원 요청함/문서함 목록 1024px+ 2열 그리드, 홈 데스크톱 2컬럼(contentGrid). 모바일=가독성: 1열 카드 유지.

### 직원 홈 개선
- 출근하기 동작: 출근(시각 표시·토스트) → 퇴근하기 → 퇴근 완료 3단계.
- 특별 지시 카드 클릭 → 내용 팝업 + "완료했어요" 처리(토스트). 스크롤 잠금 적용.
- 내 요청 현황 섹션(최근 3건+상태 뱃지, 전체 보기), 요청하기 바로가기 유지.
- `requests/new`: `?type=` 쿼리로 유형 프리셀렉트(Suspense 래핑). 데모 답변 mock 추가(mockReplies r3, 요청 2).

### 사장 측 추가
- 출근 현황 페이지에 **직원 스케줄 입력**: FAB(시계 아이콘) → 팝업에서 재직 직원별 요일 토글+출퇴근 시간 입력, 저장 시 토스트.
- 가입 신청 수락(토스트)/거절(컨펌+토스트).

### setTimeout 전면 제거 (사용자 피드백 → good-bad.md 등록)
- 제거: requests/new 제출 지연 이동, RequestDetailView 삭제 지연, 직원 초대 지연 닫기, 직원 상세 '저장됨' 리셋, checklists 반복 퇴장 애니메이션 타이머. 전부 즉시 처리+토스트로 대체. 예외: ToastContext 내부 자동 사라짐 타이머.

### 스크롤 잠금 일관화
- useScrollLock을 attendance(시트/스케줄 팝업), employees(초대 시트), checklists(관리/수행방법/담당자/카테고리 팝업), documents(카테고리 팝업), requests(직원 선택 팝업), employee 홈(특별지시 팝업)에 적용.

### 검증
- `tsc`, `eslint` 통과. `npm run build` 성공(20개 라우트). 전 라우트 curl 200 확인.

## 스케줄 모델 재설계: 사장이 생성 + 일자별 조정 (후속)

### 요구
- 기존 팝업은 "근무시간 관리" 수준. 스케줄은 **사장이 생성해야 존재**(기본 없음)하고, **일자별 조정**도 가능해야 함.
- 테스트 시 3000 포트 금지(사용자 실행 중) → good-bad.md 등록. Next dev는 동일 디렉토리 중복 실행이 불가하므로 서버 검증은 tsc/eslint로 대체.

### 변경
- `src/mock/employees.ts` — 최수아(emp5, ACTIVE, **schedule 없음**) 추가. 요청 mock에만 있고 직원 목록에 없던 불일치 해결 + "스케줄 미생성" 상태 시연용.
- `src/app/owner/attendance/page.tsx` — 스케줄 팝업을 2탭으로 재설계:
  - **기본 스케줄 탭**: 직원별 `WeeklySchedule | null`. null이면 "등록된 스케줄이 없습니다 + 스케줄 만들기"(생성 시 빈 요일로 시작), 있으면 요일 토글+시간 편집+삭제(컨펌).
  - **일자별 조정 탭**: 날짜 선택 → 직원별 해당 날짜의 실효 상태(`effectiveDay`: 조정 > 기본 패턴 순). 근무/휴무 세그먼트 토글, 근무 시간 변경(그 날짜만 override), "조정됨" 뱃지 + "기본으로" 리셋. 기본 스케줄은 불변.
  - overrides 구조: `Record<날짜, Record<직원, {off, startTime, endTime}>>`.
- CSS: 탭/생성 버튼/세그먼트/조정 뱃지/날짜 행 스타일 추가.

### 검증
- `tsc`, `eslint` 통과. (dev 서버는 사용자 3000 실행 중으로 병행 기동 불가 — 사용자 서버에서 확인 가능)

## 직원 이름 클릭 → 프로필 팝업 전역 적용 (후속)

### 변경
- `src/components/EmployeeName.tsx` / `.module.css` (신규) — 직원 이름 공통 컴포넌트. `mockEmployees`에서 이름 매칭되면 클릭 시 `EmployeeProfilePopup`, 아니면(사장·가입신청자 등) 일반 텍스트. 행 전체가 버튼/링크인 곳에서도 쓰도록 `span role="button"` + preventDefault/stopPropagation. hover 시 primary+underline.
- 적용 위치:
  - 사장: 홈 요청 목록 작성자, 요청함(카드/리스트/칸반) 작성자, 요청 상세 댓글 작성자, 출근 현황 타임라인 이름·스케줄 팝업 이름, 업무리스트(직원 패널·배정 칩·툴팁·담당자 목록), 문서함 목록·상세 작성자.
  - 직원: 홈 인사말 이름, 요청 상세 댓글 작성자, 문서함 목록·상세 작성자.
  - 기존 팝업 유지: 사장 홈 출근현황 이름, 요청 상세 '요청자' 버튼.
- z-index 계층 정리: 홈 상세 Modal(500) 위에 뜨도록 `EmployeeProfilePopup` 오버레이 200→**700**, `RequestDetailView` 내부 팝업(상태/유형/공개범위) 200→**600**. (Toast 9999 최상위 유지)

### 검증
- `tsc`, `eslint` 통과.

## 홈 팝업 → 상세화면 재사용 + 상태 변경 가능 (후속)

### 결정 (사용자 확인)
- 요청 팝업은 "상세화면 통째로" 재사용. 직원 프로필은 요청 상세의 프로필 팝업 재사용. 팝업 내 상태 변경 가능.

### 신규/이동 파일
- `src/components/EmployeeProfilePopup.tsx` / `.module.css` (신규) — 요청 상세의 직원 프로필 팝업을 공용 컴포넌트로 추출. 요청 상세 + 홈 출근현황에서 공유.
- `src/app/owner/requests/RequestDetailView.tsx` (신규) — 요청 상세 본문/댓글/상태·유형·공개범위 변경/활동내역 전체를 컴포넌트화. `mode: 'page' | 'modal'`.
- `src/app/owner/requests/RequestDetailView.module.css` — 기존 `[id]/page.module.css` 를 git mv, `.viewModal` 추가.
- `src/app/owner/documents/DocumentDetailView.tsx` (신규) — 문서 상세(보기/수정/삭제) 컴포넌트화. `mode` 지원.
- `src/app/owner/documents/DocumentDetailView.module.css` — 기존 `[id]/page.module.css` git mv, `.viewModal`·`.modalActionBar` 추가.
- `src/app/owner/requests/[id]/page.tsx`, `src/app/owner/documents/[id]/page.tsx` — 얇은 래퍼(`<XDetailView mode="page" />`)로 축소.

### 변경 내용
- 홈 요청/문서 팝업: `Modal size="wide" flush` 안에 `RequestDetailView`/`DocumentDetailView`(`mode="modal"`)를 렌더 → 라우트 페이지와 100% 동일한 디자인. 팝업 안에서 상태/유형/공개범위 변경, 댓글, 삭제 동작(삭제 시 모달 닫힘).
- 홈 직원 이름 클릭 → `EmployeeProfilePopup`(요청 상세와 동일).
- `Modal`에 `size`('default'|'wide')·`flush`(회색 배경·무패딩) 옵션 추가.
- 홈에서 직접 만들었던 읽기전용 요약 모달 마크업/CSS 제거(요청/문서/직원). 가입 신청 목록 팝업은 유지.

### 검증
- `tsc`, `eslint` 통과. 홈·요청상세(`/owner/requests/2`)·문서상세(`/owner/documents/doc1`) 라우트 모두 200 및 핵심 요소(요청 관리/상태 변경/활동 내역, 문서 제목) 렌더 확인.

### 팝업 크기 조정 (후속)
- 직원 프로필 제외하고 팝업 확대 요청 → `Modal.module.css`: wide `760→960→1440px`, default(가입 신청) `440→560→840px` (1.5배). max-height 90~92dvh.

### 팝업 고정 크기 + 내부 채우기 (후속)
- 내용 양과 무관하게 팝업 크기 고정 요청.
- `Modal.module.css`: `max-width/max-height` → **고정 크기**. default `width: min(840px,92vw); height: min(760px,88dvh)`, wide `width: min(1440px,94vw); height: 90dvh`. `.body`에 `flex:1; min-height:0`로 남는 높이를 채우고 내부 스크롤.
- `RequestDetailView.module.css`/`DocumentDetailView.module.css`의 `.viewModal`에 `min-height:100%` 추가(짧은 내용도 팝업 높이를 채우고, 길면 내부 스크롤). 문서 읽기 본문 `.body`는 `flex:1`로 채움.
- 결과: 요청/문서 팝업이 콘텐츠 길이와 무관하게 동일 크기로 열리고 내부가 팝업을 채움.

### 팝업 열렸을 때 배경 스크롤 잠금 (후속)
- 팝업이 열리면 휠이 뒤 페이지가 아닌 팝업 내부에서만 동작하도록 body 스크롤 잠금.
- `src/lib/useScrollLock.ts` (신규) — `active`인 동안 `document.body.overflow='hidden'`. 겹친 팝업 안전하게 참조 카운트로 관리, 마지막 해제 시 원복.
- 적용: `Modal`(요청/문서/가입 신청), `EmployeeProfilePopup`(직원 프로필), `RequestDetailView` 인라인 팝업(상태/유형/공개범위 변경). → 팝업 내부는 `.body`/`.viewModal` overflow로 스크롤, 배경은 고정.

### 요청 팝업 우측 패널 정렬 (후속)
- 원인: `.sidePanel`이 `position:sticky; top:var(--header-height)`(70px)인데, 라우트엔 70px sticky 헤더가 있어 맞지만 모달엔 헤더가 없어 우측 패널 자연 위치(28px)가 top(70px)보다 작아 sticky가 즉시 아래로 밀어냄.
- 수정: `RequestDetailView.module.css`에 `.viewModal .sidePanel { top: 16px }` 추가 → 모달 안에서 우측 패널이 본문 상단과 같은 높이에서 시작. (라우트 페이지는 영향 없음)

### 대시보드 문서함 카테고리 뱃지 제거 (후속)
- 홈 문서함 패널의 카테고리 칩 목록(`categoryChips`/`categoryChip`) 제거. 최근 업데이트 문서 목록만 남김. 관련 CSS도 정리(`DOCUMENT_CATEGORIES`는 `categoryName`에서 계속 사용).

### 요청현황 목록 행 정리 (후속)
- `.requestContent`: **한 줄 고정** (`white-space:nowrap; overflow:hidden; text-overflow:ellipsis`), 넘치면 `…` 말줄임 → 행 높이 불변. (초기엔 2줄 고정으로 했다가 한 줄로 변경)
- 콘텐츠 `<span>`에 `title={req.content}` 추가 → 마우스 오버 시 전체 내용 툴팁.
- `.requestType` 뱃지 크기 통일: `display:inline-flex; width:56px; justify-content:center` → 유형 글자 수와 무관하게 동일 크기.

### 요청현황 요약 3종 변경 (후속)
- `장비 이상`(장비고장 & !DONE) → `확인됨`(CONFIRMED)으로 교체. 요약 3개: 미확인(REQUESTED) / 확인됨(CONFIRMED) / 처리 중(IN_PROGRESS).
- `RequestFilter` 타입/필터 로직/버튼 라벨 갱신. 기본 선택 미확인 유지.

## 사장(owner) 홈 화면 대시보드 재구성

### 요청
홈에서 업무리스트·요청함·문서함·직원관리(가입신청 현황)·스케줄(오늘자 출근자)을 한 번에 확인할 수 있도록, 화면을 가득 채우는 구성으로 수정.

### 변경 파일
- `src/app/owner/page.tsx`
- `src/app/owner/page.module.css`

### 변경 내용
- 기존 홈(출근 현황 / 업무 현황 / 요청 현황 3개 영역)에 **문서함**과 **가입 신청** 영역을 추가하여 총 5개 영역으로 확장.
  - 문서함: `DOCUMENT_CATALOG` 총 건수, 카테고리 칩(`DOCUMENT_CATEGORIES`), 최근 업데이트 문서 4건(updatedAt 내림차순).
  - 가입 신청: `mockJoinRequests` 목록 + 대기 건수 배지, 각 신청자 이름·연락처·신청일.
  - 출근 현황 헤더에 출근 완료/전체 카운트 추가.
- 각 영역을 공통 `.panel`(헤더 + 전체보기 링크 + 바디) 구조로 통일하고, 헤더 링크를 해당 메뉴로 연결(`/owner/attendance`, `/owner/checklists`, `/owner/requests`, `/owner/employees/join-requests`, `/owner/documents`).
- **화면 가득 채우기**: 페이지를 flex column + `min-height:100dvh`, 그리드를 `flex:1`로 두어 뷰포트를 채우는 벤토(bento) 레이아웃 적용.
  - 모바일: 1열 스택.
  - 태블릿(≥768): 2열 (attendance/tasks, requests/joins, documents 전폭).
  - 데스크톱(≥1024): 3열 2행, 출근 현황이 좌측 2행 span, `grid-template-rows: auto 1fr`로 하단 행이 남는 높이를 채움.
- 넘칠 수 있는 패널 바디는 `overflow-y:auto`로 내부 스크롤 처리.

### 결정 사항
- 스케줄 "오늘자 출근자"는 기존 `mockAttendance` 데이터를 그대로 활용(별도 신규 데이터 없음).
- 트랜잭션/스키마/타입 변경 없음. 목업 데이터만 참조.

### 검증
- `tsc --noEmit`, `eslint` 통과.
- dev 서버 `/owner` 200 응답, 5개 영역·가입신청자·최근 문서 렌더링 확인.

## 홈 요청 현황 - 인터랙션 추가 및 확인 필요 제거 (후속)

### 변경 파일
- `src/app/owner/page.tsx`
- `src/app/owner/page.module.css`

### 변경 내용
- **"확인 필요" 알림 영역 제거**: 관련 JSX·`alertItems` 계산 로직·CSS(`.alertSection` 등) 삭제.
- **요청 현황 요약 클릭 → 내역 목록 표시**:
  - 미확인/처리 중/장비 이상 3개 요약을 `button`으로 변경, `useState`로 선택 필터(`pending`/`inProgress`/`equipment`) 관리.
  - 같은 항목 재클릭 시 토글로 목록 닫힘.
  - 선택 시 하단에 해당 요청 목록(유형 배지 + 내용 2줄 클램프 + 신청자·일시) 표시, 각 행은 `/owner/requests/[id]` 상세로 이동.
  - 미선택 시 안내 문구, 결과 없을 시 빈 상태 문구.
  - 활성 요약은 primary-light 배경 + 하단 보더로 강조.

## 홈 레이아웃 재배치 및 가입 신청 축소 (후속)

### 변경 파일
- `src/app/owner/page.tsx`
- `src/app/owner/page.module.css`

### 변경 내용
- **컬럼 재배치** (데스크톱 3열 2행):
  - 왼쪽: 요청 현황(2행 span)
  - 가운데: 업무 현황(2행 span)
  - 오른쪽: 문서함(상) + 출근 현황(하)
  - `grid-template-areas: "requests tasks documents" / "requests tasks attendance"`, `rows: 1fr 1fr`.
  - 태블릿(≥768) 2열: `"requests tasks" / "documents attendance"`.
  - 모바일: 요청→업무→문서함→출근 순 스택(DOM 순서 재정렬).
- **가입 신청 독립 패널 제거** → 출근 현황 패널 헤더 아래 **건수만** 표시하는 요약 행(`가입 신청 · N건`)으로 축소. 클릭 시 `/owner/employees/join-requests` 이동.
- 미사용 스타일 정리(`.joinRow`, `.joinBadge`, `.countBadge`, `areaJoins`).

## 홈 인터랙션 보강 (후속)

### 변경 파일
- `src/app/owner/page.tsx`
- `src/app/owner/page.module.css`

### 변경 내용
- **요청 현황 기본 선택**: `requestFilter` 초기값을 `null` → `'pending'`(미확인)으로 변경. 진입 시 미확인 목록이 바로 표시.
- **문서함**: 헤더 `N건` 뱃지 제거. 최근 문서 행을 `Link`로 변경해 제목 클릭 시 `/owner/documents/{doc.id}` 상세 이동(hover 시 제목 강조).
- **출근 현황 이름 → 프로필**: 출근 데이터의 `employeeId`(`'1'/'2'`)가 직원 프로필 ID(`emp*`)와 달라, `mockEmployees`에서 **이름으로 매칭**하여 `/owner/employees/{emp.id}`로 링크. 매칭 실패 시 기존 텍스트 유지.

### 검증
- `tsc`, `eslint` 통과. `/owner` 200 + 미확인 활성/요청·문서·직원 상세 링크 렌더링 확인.

## 홈 항목 클릭 → 팝업(모달) 전환 (후속)

### 결정 (사용자 확인)
- "전체 보기" 링크는 **페이지 이동 유지**, 항목 클릭만 팝업.
- 팝업 내용은 **읽기 전용 요약** (수정/삭제 등 액션 없음).

### 변경 파일
- `src/components/Modal.tsx` (신규) — 공통 모달: 오버레이 클릭·ESC 닫기, 헤더(제목+닫기), 스크롤 바디.
- `src/components/Modal.module.css` (신규) — `ConfirmContext` 오버레이 패턴 참고.
- `src/app/owner/page.tsx`
- `src/app/owner/page.module.css`

### 변경 내용
- 홈의 상세 이동(`Link`)을 모두 `button` + 모달 오픈으로 전환:
  - 요청 행 → 요청 상세 팝업(유형·상태·작성자·내용·사진 첨부 여부·`mockReplies` 답변).
  - 문서 제목 → 문서 팝업(카테고리·수정일·작성자 + 본문 HTML `dangerouslySetInnerHTML` 읽기 전용).
  - 출근 현황 직원 이름 → 직원 프로필 팝업(연락처·생년월일·상태·입사/퇴사일·근무 스케줄).
  - 가입 신청 요약 → 가입 신청 목록 팝업(이름·연락처·신청일·메시지).
- 모달 상태는 discriminated union(`HomeModal`)으로 단일 `useState` 관리.
- `button` 유효성 위해 요청 행 내부 `div/p`를 `span`(display:block)으로 변경. 버튼 클래스에 `width:100%`·`text-align:left` 추가.
- "전체 보기"(요청함/업무리스트/문서함/출근현황) 4개 링크는 페이지 이동 그대로 유지.
- 제거된 알림 영역의 잔여 CSS(`.alertSection` 미디어쿼리 규칙) 정리.

### 검증
- `tsc`, `eslint` 통과. `/owner` 200. 항목 상세용 `href` 제거(버튼화) 및 "전체 보기" 링크 4개 유지 확인. 모달 본문은 클릭 시 클라이언트 렌더.
