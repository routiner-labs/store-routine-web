# 2026-07-07 작업 기록

## 출근 현황 날짜 상세 시트 - 프로필 팝업 음영 버그 수정

### 증상
월간 캘린더에서 날짜 클릭 → 바텀시트(날짜 상세) → 직원 이름 클릭 → 프로필 팝업이 떴을 때, 다른 직원들의 이름 부분만 어두운 오버레이(음영)가 적용되지 않고 밝게 보임.

### 원인
타임라인의 이름 컬럼 `.tlLabelCol`이 `position: sticky; z-index: 1; background: surface`로 스태킹 컨텍스트를 생성. `EmployeeName`이 띄우는 `EmployeeProfilePopup`(z=700)이 클릭한 행의 `.tlLabelCol` 내부에 렌더되어 그 컨텍스트(z=1)에 갇힘 → DOM상 뒤에 오는 다른 행들의 `.tlLabelCol`(z=1, 불투명 배경)이 오버레이 위에 그려짐.

### 수정
- `src/components/EmployeeProfilePopup.tsx` — `createPortal(..., document.body)`로 렌더. 어떤 중첩 위치(sticky/transform 등 스태킹 컨텍스트 내부)에서 열려도 항상 문서 최상위에 뜸. `EmployeeName`을 쓰는 모든 위치에 공통 적용되는 근본 수정.

### 검증
- `tsc`, `eslint` 통과. (3000 포트는 사용자 dev 서버 실행 중 — 해당 서버에서 확인)

## 문서함 상세 페이지 디자인 개편

### 요구/방향
- 기존 디자인이 밋밋함 → 개선. 단 화려한 장식 없이 **정적인 느낌**. 웹에서는 **화면 전체 폭** 사용.
- (중간에 카테고리 색 액센트 바·박스형 리스트 시안을 만들었다가 "정적인 느낌" 피드백으로 제거)

### 변경 파일
- `src/app/owner/documents/DocumentDetailView.tsx` / `.module.css`
- `src/app/employee/documents/[id]/page.tsx` / `.module.css` (동일 디자인 적용)

### 변경 내용
- 회색 배경 위 **조용한 문서 카드**(흰 배경, 1px 보더, radius-lg, 그림자 없음) 구조로 변경: `docHead`(카테고리 뱃지, 제목, 작성자+날짜 메타 라인) + 헤어라인 구분선 + `docContent`.
- 타이포그래피 중심: 제목 23px/자간 -0.2, 본문 line-height 1.9, 문단 간격 16px, h2 담백한 볼드, 리스트 기본 마커(secondary 색), 이미지 보더+라운드.
- 웹: `.body` max-width 제거 → 전체 폭(`--page-x` 패딩만). 모바일: 패딩·제목 축소로 가독성 유지.
- 직원 상세는 "내 문서" 뱃지 포함 동일 구조. 중복 `.meta` 정의 등 잔여 CSS 정리.
- 모달(홈 팝업) 내부도 회색 배경 위 카드로 통일.

### 검증
- `tsc`, `eslint` 통과.

## 퇴직 처리 절차 추가

### 변경
- `src/app/owner/employees/[id]/page.tsx` / `.module.css` — 퇴직 처리 버튼(좌측/모바일)과 고용 상태 토글의 '퇴직'이 모두 전용 절차 팝업을 거침:
  - "정말로 OOO님을 퇴직 처리할까요?" 제목 + 경고 목록(스케줄/근무 일정 삭제, 업무리스트 배정 제외, 요청·문서 기록 유지, 복직 방법).
  - 퇴직일 입력(date) + "위 내용을 확인했으며 동의합니다" 체크박스 — 체크해야 버튼 활성화.
  - 확인 시 상태 INACTIVE + 퇴직일 기록(프로필에 표시) + 스케줄 요일 초기화 + 토스트. 버튼 배치는 good-bad 규칙(확인 왼쪽/취소 오른쪽).

## 업무리스트: 리스트 테스크에 수행 시간 지정

### 변경
- `src/app/owner/checklists/page.tsx` / `.module.css` — 업무 제목 드롭다운 메뉴에 "수행 시간 지정" 추가. 팝업에서 기존 `TimeRangeSlider`로 시작~종료 설정(상시로 해제 가능). 선택한 날짜 목록의 해당 업무에만 적용(카탈로그 템플릿 불변). 저장 시 토스트.

## 대시보드 업무 현황을 업무리스트 데이터와 동기화

### 변경
- `src/app/owner/page.tsx` — 기존 오픈/마감 체크리스트·특별 지시(mockChecklists) 대신 업무리스트 페이지와 동일한 `createTasksForDate('2026-06-30')` 기반으로 변경: 공통 업무 진행률, 추가 업무 진행률, 미할당 업무 N건(0이면 '없음', >0이면 붉은 강조).

## 수행 방법 보강 + 문서함 문서 참조 구조

### 변경
- `src/mock/tasks.ts` — `TaskTemplate.docRefs?: string[]` / `StoreTask.docRefs` 추가. 전 테스크의 `method`를 게시글형 HTML(도입 문단 + 순서 목록)로 보강하고 관련 문서 연결(t1→doc9, t2→doc1·doc2, t5→doc10·doc12, t6→doc13·doc16, t8→doc14, t9→doc11, t10→doc8 등).
- `src/app/owner/checklists/page.tsx` / `.module.css`:
  - 수행 방법 팝업에 참조 문서 섹션 — 카테고리 뱃지+제목 행, 클릭 시 `Modal(wide)`+`DocumentDetailView(mode="modal")`로 문서함 상세와 동일하게 열람.
  - 테스크 관리 폼에 참조 문서 필드 — "문서 추가하기" → 검색 가능한 문서 선택 팝업(체크 토글), 선택 목록 표시/해제. 저장 시 카탈로그·해당 날짜 리스트에 반영.
  - methodContent 타이포를 문서 상세와 톤 맞춤(line-height 1.8, 문단/목록 간격).

## 요청함 항목 → 테스크 생성 + 업무리스트 추가

### 변경
- `src/mock/tasks.ts` — `registerTaskFromRequest({title, method, category, kind})`: 카탈로그에 단건 테스크 push + 공통/추가 기본 리스트에 추가(모듈 싱글턴이라 업무리스트 페이지 진입 시 반영, 새로고침 시 초기화).
- `src/app/owner/requests/RequestDetailView.tsx` / `.module.css` — 요청 관리 카드에 "업무리스트에 추가" 버튼 → 팝업: 테스크 이름(요청 내용 24자 프리필), 카테고리(요청 유형 매핑: 재료부족→재고, 청소시설→청소, 장비고장→안전), 공통/추가 리스트 선택. 생성 시 요청 내용+요청자 정보가 수행 방법으로 저장, 활동 내역 기록, 토스트.

### 검증
- `tsc` 통과, `eslint` 기존 경고(`_date` 미사용) 외 클린.

## 요청함발 테스크: 요청 참조 + 수행방법 작성 + 문서 참조 (후속)

### 변경
- `src/mock/tasks.ts` — `TaskTemplate/StoreTask.requestRef?: string`(원본 요청 id) 추가. `registerTaskFromRequest`가 `docRefs`, `requestRef`를 받아 저장.
- `src/app/owner/requests/RequestDetailView.tsx` / `.module.css` — "업무리스트에 추가" 팝업 확장(`popupWide` 640px):
  - 수행 방법 RichTextEditor(요청 내용 프리필, 자유 편집). ref 렌더 중 접근 lint 에러는 초기값 state 분리로 해결.
  - 참조 문서 선택: "문서 추가하기" → 검색+체크 문서 선택 팝업, 선택 목록 표시/해제.
  - 생성 시 requestRef=현재 요청 id 자동 연결.
- `src/app/owner/checklists/page.tsx` — 수행 방법 팝업에 "참조 요청" 섹션: 요청 유형 뱃지+내용 행, 클릭 시 `Modal(wide)`+`RequestDetailView(mode="modal")`로 요청함 상세와 동일하게 열람(상태 변경 등 가능). checklists modalOverlay(z=300) < Modal(500)이라 참조 팝업이 위에 뜸.

### 검증
- `tsc` 통과, `eslint` 클린(기존 `_date` 경고 제외).

## 요청함 → 업무리스트 추가: 중복 방지 + 활동 내역 표기 (후속)

### 변경
- `src/types/index.ts` — `ActivityType`에 `TASK_ADDED` 추가.
- `src/mock/tasks.ts` — `hasTaskForRequest(requestId)`: 해당 요청으로 만든 테스크(requestRef 매칭) 존재 여부 확인.
- `src/app/owner/requests/RequestDetailView.tsx` / `.module.css`:
  - 이미 추가된 요청이면 버튼 비활성화 + 라벨 "업무리스트에 추가됨"(disabled 스타일). 페이지 이탈/재진입에도 카탈로그(모듈 싱글턴) 기준으로 유지.
  - 활동 내역을 `TASK_ADDED` 타입으로 기록 — "사장님이 업무리스트에 추가했습니다 (추가 업무)" 형태로 표시(기존 CONTENT_EDITED 오표기 수정). 타임라인 보라색 dot 추가.

### 검증
- `tsc`, `eslint` 클린.

## 활동 내역 한 줄 말줄임 (후속)
- `.timelineText`: 한 줄 고정(`white-space:nowrap; overflow:hidden; text-overflow:ellipsis`), 넘치면 `…`. `title` 속성으로 마우스 오버 시 전체 내용 툴팁. (`.timelineContent`에 `min-width:0` 이미 존재)

## 요청 삭제 시 연결 테스크 동반 삭제 + 활동내역 호버 툴팁 (후속)

### 변경
- `src/mock/tasks.ts` — `removeTasksForRequest(requestId)`: 요청으로 만든 테스크를 카탈로그·기본 리스트(공통/추가)에서 제거.
- `src/app/owner/requests/RequestDetailView.tsx` — 요청 삭제 컨펌: 연결 테스크가 있으면 "이 요청으로 만든 업무리스트 테스크도 함께 삭제됩니다" 메시지로 분기, 확인 시 테스크 동반 삭제 + 전용 토스트.
- 활동 내역 툴팁을 네이티브 `title`(지연 있음) → `data-tooltip` + CSS `::after` 커스텀 툴팁으로 교체: 마우스 호버 즉시 말풍선(다크 배경, 최대 240px 줄바꿈) 표시. 기존 칸반 컬럼 툴팁과 동일 패턴.

### 검증
- `tsc`, `eslint` 클린(기존 `_date` 경고 제외).

### 활동내역 툴팁 미표시 수정 (후속)
- 원인: `.timelineText`의 `overflow:hidden`(한 줄 말줄임)이 자기 자신에 붙인 `::after` 툴팁까지 클리핑.
- 수정: `data-tooltip`/`::after`를 overflow 없는 부모 `.timelineContent`(position:relative 추가)로 이동. 행 어디에 호버해도(짧든 길든) 즉시 툴팁 표시. `visibility` 토글 추가.

## 요청함 댓글 수정/삭제 (후속)

### 변경
- `src/app/owner/requests/RequestDetailView.tsx` / `.module.css` — 댓글 헤더에 수정/삭제 아이콘:
  - 수정: 사장 본인 댓글만. 인라인 편집(textarea + 저장/취소) 후 토스트.
  - 삭제: **직원 댓글 포함 모든 댓글 삭제 가능**(사장 권한). 컨펌("삭제된 댓글은 되돌릴 수 없습니다") + 토스트.
- `src/app/employee/requests/[id]/page.tsx` / `.module.css` — 직원은 본인(이지은) 댓글만 수정/삭제. 동일한 인라인 편집/컨펌/토스트 패턴.

### 검증
- `tsc`, `eslint` 클린.

## 대시보드 업무 현황: 리스트 펼침 (후속)
- 공통/추가 업무 리스트 행을 버튼으로 변경, 클릭 시 아래로 해당 리스트의 업무 목록 펼침(재클릭 시 접기, 한 번에 하나만).
- 목록 행: 완료 체크(초록 원) + 제목(한 줄 말줄임 + title 툴팁) + 담당자("김민수 외 1명") 또는 "미할당" 붉은 뱃지.
- 셰브론 회전 표시, 회색 배경 서브리스트. 미할당 업무 요약 행은 유지.
- 펼침 애니메이션 추가: `taskSubListDown` 0.2s(슬라이드 다운 + 페이드 인), 셰브론 90도 회전 트랜지션. 기존 드롭다운 패턴과 동일한 톤.
- 닫힘 애니메이션 추가: 마운트 keyframe → `grid-template-rows 0fr↔1fr` 트랜지션으로 교체(항상 렌더 + 클래스 토글). 열림/닫힘 모두 높이+페이드+슬라이드 애니메이션. setTimeout 없이 순수 CSS.
